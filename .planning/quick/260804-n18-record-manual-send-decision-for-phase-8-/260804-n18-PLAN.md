---
phase: quick
plan: 260804-n18
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .claude/CLAUDE.md
  - .planning/research/SEND-CHANNEL.md
autonomous: true
requirements: [SND-01, SND-02, SND-04, CMP-02, CMP-09, CMP-11, CMP-12]

estimate:
  tokens: 56000
  raw_tokens: 37000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "SND-02 in REQUIREMENTS.md states a fast, free, one-step opt-out and no longer states an RFC 8058 header requirement, with the rationale recorded beside it"
    - "The Send section note records the provider half of the gate as closed and the legal half as still gating Phase 8"
    - "SND-01 and SND-04 carry annotations explaining they are satisfied trivially by manual send, with SEND-CHANNEL.md named as the SND-04 evidence artifact"
    - "PROJECT.md Legal constraint matches the new SND-02 wording and still ends with the words Non-negotiable"
    - "PROJECT.md Provider policy constraint records that the resolution was removing the third-party provider from the send path, and keeps the AUP-check principle for any future automated channel"
    - "ROADMAP Phase 8 success criteria 1 and 2 describe manual send and the body-link opt-out, and the phase is still marked blocked on the legal gate"
    - "The Parallel Track provider row is closed as of 2026-08-04, the counsel row is open, the warmed-domain row is marked moot and is not deleted"
    - "SEND-CHANNEL.md leads with the manual-send decision, marks its own recommendation superseded in place, and keeps the tier comparison whole"
    - "PROJECT.md's Key Decisions table carries a row for the manual-send decision, placed directly after the Resend row it supersedes, in the table's existing three-column shape"
    - "No application code, test, migration, or config file is touched"
  artifacts:
    - ".planning/REQUIREMENTS.md"
    - ".planning/ROADMAP.md"
    - ".planning/PROJECT.md"
    - ".claude/CLAUDE.md"
    - ".planning/research/SEND-CHANNEL.md"
  key_links:
    - "SND-02 wording in REQUIREMENTS.md, the PROJECT.md Legal constraint, and ROADMAP success criterion 2 must all describe the same opt-out. Three copies of one sentence, drift between them is the failure mode"
    - "SND-04 points at .planning/research/SEND-CHANNEL.md as the named evidence artifact, so that file must survive intact"
    - "CMP-02's phrase immediately before dispatch is redefined in the ROADMAP notes as at the moment the draft is prepared for copying. The requirement ID and its text stay unchanged"
    - ".claude/CLAUDE.md carries a verbatim copy of the PROJECT.md Constraints bullets and is auto-loaded every session. If only one copy is edited, every future session loads the contradicted version"
    - "The Key Decisions row sits directly below the row it supersedes. A decision table that omits the decision which overturned the row above it is worse than no table"
---

<objective>
Record the manual-send decision for Phase 8 across the planning artifacts, so the written
record matches what was actually decided on 2026-08-04.

The decision, locked and not up for revisiting: there is no third-party dispatch provider.
Prospect Radar generates, renders, and gates the approved draft. Joshua then sends it by
hand from his own mailbox. Volume is 10 to 50 sends per week.

Purpose: the provider half of the Phase 8 gate is closed. Right now every planning
artifact still says the channel is deliberately undecided, and SND-02 still demands
RFC 8058 headers that a manual mail client cannot set. Left alone, the next person to plan
Phase 8 builds against a decision that was reversed.

Output: five updated documentation files. Zero code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/SEND-CHANNEL.md
</context>

<standing_instructions>

Read these before the first edit. They apply to all three tasks.

**Documentation only.** No file outside the five in `files_modified` may change. If you find
yourself opening anything under `app/`, `lib/`, `components/`, `supabase/`,
`scanner-service/`, or `types/`, or touching any `.ts`, `.tsx`, `.sql`, `.json`, `.mjs`, or
`.js` file, stop. You have misread the task.

**Preserve every requirement ID.** SND-01 through SND-04, CMP-02, CMP-09 through CMP-12.
No renumbering, no new IDs. Do not invent a `D-NN` decision ID for the manual-send decision
either. Phase 8 has not run its discuss step yet and would collide with anything invented
here. Record the decision by date, not by ID.

**Do not change checkbox state anywhere.** REQUIREMENTS.md must end with 58 `[x]` and 9
`[ ]` lines, exactly as it starts. ROADMAP.md phase checkboxes stay as they are. Phase 8 is
not complete and has not started. SND-01 and SND-04 get annotations, not ticks.

**Preserve existing headings and table row titles byte for byte.** The Phase 8 heading, the
Phase 8 progress row title, and the `current_phase_name` value in STATE.md all have to keep
matching each other for GSD tooling to find the phase. Change cell contents, never row
titles or heading text.

**House style, enforced on prose you write.** No em-dashes. Use commas, colons,
parentheses, or separate sentences. Plain English, no buzzwords. Existing text you are not
editing keeps its punctuation. Do not sweep the file for style.

**Bold list labels stay on one line with no second colon inside the bold.** This repo's
decision-coverage parser is line based and a wrapped or double-colon bold label breaks it.
Write `**Status of this document.**` on its own line, then the prose beneath it. Never
`**Status: of this document: ...**` and never a bold label that wraps.

**Correct-the-falsehood rule.** Inside the five files listed above, any sentence that now
states a factual falsehood about the provider decision gets corrected in the same pass.
That is the same decision being recorded, not new scope. The known instances are called out
in the tasks below. Sentences that are merely historical, for example the finding that
Resend's AUP prohibits cold outreach, stay exactly as they are. That finding is still true
and is still why Resend is out.

</standing_instructions>

<tasks>

<task type="tracer">
  <name>Task 1: Rewrite SND-02 and the Send gate note in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
This is the source of record for the requirement text. Every other file in this plan quotes
it, so it goes first and the later tasks copy from it rather than paraphrasing.

Three edits, all inside the `### Send — GATED` section (currently lines 93 to 101).

**Edit A, the gate note.** Replace the two-line blockquote that currently says the phase is
blocked on the send-path decision, that Resend is ruled out by its AUP, and that channel and
provider are deliberately undecided. The replacement must be precise that only half the gate
lifted. Do not write anything that reads as the phase being unblocked. Cover, in this order:
the provider half is closed as of 2026-08-04, there is no third-party dispatch provider,
Prospect Radar generates and renders and gates the draft while Joshua sends it by hand from
his own mailbox at 10 to 50 sends per week, Resend stays where it is on the public scanner's
transactional mail only, and the legal half is still open and still gates this phase. Name
the three open legal items explicitly: the Telecommunicatiewet art. 11.7 question, the
Legitimate Interest Assessment, and the Article 14 notice wording. Close by naming
`.planning/research/SEND-CHANNEL.md` as the evidence trail for the provider half.

**Edit B, SND-02 itself.** Replace the requirement line verbatim with:

`- [ ] **SND-02**: Every message carries a working opt-out that is fast and free and takes one step`

Keep the unchecked box. Keep the ID bold and unchanged.

Then add a rationale block immediately after the four SND lines, as a blockquote, with the
bold label alone on its own line. It records: RFC 8058 `List-Unsubscribe` and
`List-Unsubscribe-Post` headers cannot be set from a manual mail client, and they were never
a legal requirement in the first place. `LEGAL.md` §2.4 requires the opt-out to be "snel"
and "gratis" and present in every message, and never mentions those headers at all. The RFC
8058 requirement entered this plan through the Google and Yahoo bulk-sender rules, which
start at 5,000 messages per day to Gmail addresses, roughly 1,000x this project's volume.
SND-02 is now met by a plain opt-out link in the message body, pointing at the unsubscribe
endpoint Phase 2 already built and already ships. State that if an automated channel is ever
adopted the headers come back cheaply, because that same endpoint already implements the
one-click POST path correctly (see `02-04-SUMMARY.md`).

**Edit C, annotate SND-01 and SND-04.** Do not rewrite either requirement line. Add a short
annotation under each, or one combined annotation covering both, recording that they are now
satisfied trivially: there is no third-party dispatch channel whose acceptable-use policy
could prohibit outreach, because sending is manual from Joshua's own mailbox. For SND-04
specifically, state that this is the written verification, and that
`.planning/research/SEND-CHANNEL.md` is the SND-04 artifact holding the full provider
comparison that produced the outcome. Leave both checkboxes unchecked. The behaviour they
describe has not been built yet.
  </action>
  <verify>
    <automated>cd "/Users/annan/Developer/Claude code/projects/website-scanner" && grep '\*\*SND-02\*\*' .planning/REQUIREMENTS.md | grep -c 'Unsubscribe-Post' | grep -qx 0 && grep '\*\*SND-02\*\*' .planning/REQUIREMENTS.md | grep -qi 'one step' && grep -c '\*\*SND-0' .planning/REQUIREMENTS.md | grep -qx 4 && grep -c '^- \[x\]' .planning/REQUIREMENTS.md | grep -qx 58 && grep -c '^- \[ \]' .planning/REQUIREMENTS.md | grep -qx 9 && grep -q '5,000' .planning/REQUIREMENTS.md && grep -q 'SEND-CHANNEL.md' .planning/REQUIREMENTS.md && echo PASS</automated>
  </verify>
  <done>
The SND-02 requirement line states a fast, free, one-step opt-out and carries no header
token. All four SND IDs are present and unrenumbered. Checkbox counts are unchanged at 58
checked and 9 unchecked. The bulk-sender threshold and the SEND-CHANNEL.md evidence pointer
both appear in the file. The gate note says the provider half closed and the legal half
still gates the phase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update the Phase 8 section, Parallel Track, and Progress row in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Copy the SND-02 wording from Task 1 rather than paraphrasing it. Three of these files carry
the same sentence and drift between them is the thing that makes this record useless.

**Success criterion 1** (currently line 384). Rewrite so it describes manual send instead of
automated dispatch through a permitting channel: Joshua approves a message, Prospect Radar
prepares it for sending, and Joshua sends it by hand from his own mailbox. No third-party
dispatch provider sits in the path, so no channel acceptable-use policy applies. Keep the
trailing `(SND-01, SND-04)` reference.

**Success criterion 2** (currently line 385). Rewrite to match the new SND-02 exactly: every
message carries a working opt-out that is fast and free and takes one step, delivered as a
link in the message body pointing at the Phase 2 unsubscribe endpoint. Keep `(SND-02)`.

Criteria 3, 4, and 5 stay as they are.

**The blocked banner** (currently line 392). Keep the phase blocked. Reword so it names the
legal gate specifically rather than the whole send-path decision, since half of it closed.
The instruction not to plan or build the phase until counsel lands stays in force.

**Notes block.** First, correct the two now-false statements inside it. The bullet declaring
channel and provider deliberately undecided keeps its Resend AUP finding, which is still
true, but loses the undecided clause and instead points at the manual-send decision. The
bullet claiming the suppression design and header pattern carry over to whatever channel is
chosen is now wrong about the headers and needs rewriting: the Phase 2 suppression spine
carries over unchanged, and the opt-out moves from a header to a body link.

Then add four new bullets:

1. The manual-send decision and its date, 2026-08-04. State it plainly and state that it
   closed the provider half of the gate.
2. CMP-02's phrase "immediately before dispatch" now means "at the moment the draft is
   prepared for copying". Be honest that a residual window remains between copying the draft
   and actually sending it. Then note that this window is smaller than the status quo it
   replaces, where a draft can sit in the approval queue for days while suppression state
   changes underneath it. Do not change the CMP-02 requirement text or its ID.
3. CMP-09, CMP-11, and CMP-12's audit record is written on an explicit mark-as-sent action
   rather than on a provider message ID. Delivery proof is lost. Legal-basis proof is not,
   and success criterion 5 only ever asked for legal-basis proof.
4. What is lost outright: automated bounce handling, reply detection, delivery confirmation.
   All three are manageable by hand at 10 to 50 sends per week.

**Parallel Track table** (currently lines 31 to 35). Add a fourth column, `Status`, updating
the header row and the separator row to match. Values:

- Counsel row: `OPEN`
- Provider/AUP row: `CLOSED 2026-08-04 (manual send, no provider)`
- Warmed outreach domain row: `Moot for manual send from an existing mailbox. Returns if an automated channel is ever adopted.`

Do not delete the warmed-domain row. Marking it moot is the point.

**Progress table** (currently line 419). Change only the Status cell, from
`Blocked (send-path decision)` to `Blocked (legal gate only)`. Leave the row title, the
plans-complete cell, and the completed cell untouched. Do not tick the Phase 8 checkbox in
the phase list at line 25, and do not touch any other row.

**Correct-the-falsehood sweep for this file.** The Overview paragraph (line 7) and the
Phase 8 one-liner (line 25) both still assert an undecided channel or dispatch through a
permitting channel. Correct both clauses to match the decision. Change nothing else in
either line, and keep the Phase 8 heading text and its checkbox exactly as they are.
  </action>
  <verify>
    <automated>cd "/Users/annan/Developer/Claude code/projects/website-scanner" && grep -q 'Blocked (legal gate only)' .planning/ROADMAP.md && grep -q 'CLOSED 2026-08-04' .planning/ROADMAP.md && grep -qi 'moot' .planning/ROADMAP.md && grep -qi 'by hand' .planning/ROADMAP.md && grep -c 'deliberately UNDECIDED' .planning/ROADMAP.md | grep -qx 0 && grep -c 'deliberately undecided' .planning/ROADMAP.md | grep -qx 0 && grep -q '^- \[ \] \*\*Phase 8' .planning/ROADMAP.md && grep -q '^### Phase 8: Send' .planning/ROADMAP.md && grep -c '^| 8\. Send' .planning/ROADMAP.md | grep -qx 1 && echo PASS</automated>
  </verify>
  <done>
Phase 8 criteria 1 and 2 describe manual send and the body-link opt-out. The phase is still
blocked, now on the legal gate alone. The Parallel Track table has a Status column with the
provider row closed, the counsel row open, and the warmed-domain row marked moot and still
present. The Progress row status reads `Blocked (legal gate only)`. No claim that the
channel is undecided survives anywhere in the file. The Phase 8 checkbox is still unticked
and the heading is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update the PROJECT.md constraints, its CLAUDE.md mirror, and supersede SEND-CHANNEL.md</name>
  <files>.planning/PROJECT.md, .claude/CLAUDE.md, .planning/research/SEND-CHANNEL.md</files>
  <action>
**PROJECT.md, two constraints** (currently lines 207 to 211).

The Legal constraint currently reads that every send passes a human gate, carries
`List-Unsubscribe`, and is checked against the suppression list first, ending
"Non-negotiable." Replace the header clause with wording matching the new SND-02: a working
opt-out that is fast and free and takes one step. Keep the human gate. Keep the
suppression-check-first clause. Keep the word "Non-negotiable" verbatim at the end. It is
still non-negotiable, only the mechanism changed.

The Provider policy constraint currently says the outreach channel must permit outreach
under its own terms, that Resend does not, and that any candidate channel is checked against
its AUP before it is built against. Update it to record that the resolution was to remove
the third-party provider from the send path entirely, as of 2026-08-04. Keep the principle
intact and explicitly forward-looking: it binds any future automated channel, checked before
it is built against, not after.

**PROJECT.md, correct-the-falsehood sweep.** Two more places in this file assert the
superseded position. The gated deliverable at line 65 says dispatch happens via a channel
that permits it with a header on every message, and that provider and channel are
deliberately UNDECIDED. The Resend paragraph at lines 183 to 186 says the outreach channel
is a separate, deliberately open decision and that the header pattern carries over to
whatever channel is chosen. Correct both to match the decision. Keep the Resend AUP finding
itself, which is unchanged and still correct. Do not tick the line 65 checkbox. Do not add a
row to the Key Decisions table in this pass, see the deferred note below.

**.claude/CLAUDE.md, lines 37 to 42.** This file carries a verbatim copy of both constraint
bullets and is auto-loaded into every session in this repo. Apply the identical replacement
text so the two copies match word for word. This is the same edit applied to a duplicate,
not a second decision. Do not touch anything else in the file, and do not touch any copy
under `.claude/worktrees/`. Those are stale worktrees and are out of scope.

**.planning/research/SEND-CHANNEL.md.** Written earlier today, currently untracked. Its
recommendation is superseded, its evidence is not.

Insert a new section at the very top, after the research-date line and before the first
horizontal rule, numbered `## 0.` so the existing section numbers stay valid. It records:

- A status statement. The comparison below stands and is the SND-04 artifact. The
  recommendation in section 3 does not stand. Nothing is built against it.
- The decision itself, dated 2026-08-04. No third-party dispatch provider. Prospect Radar
  generates, renders, and gates the approved draft. Joshua sends it by hand from his own
  mailbox, at 10 to 50 sends per week.
- Why it beats every tier below it. Five points, and each one should land against something
  the document below actually argues: it removes the acceptable-use question entirely rather
  than answering it, since every tier below is an argument about whether someone else's
  contract permits this send. It costs nothing, against €40 to €250 per month for tier 2 and
  roughly €7 per month plus a domain for the section 3 recommendation. It needs no warmup
  and no new domain, because at this volume there is no deliverability problem to solve. The
  Phase 2 suppression spine stays the single source of truth with nothing to sync and no
  consistency window, which is the exact objection section 2 raised against tier 2. The
  Phase 6 human gate is untouched, since approval was always manual and now dispatch is too.
- What it costs, stated plainly: automated bounce handling, reply detection, and delivery
  confirmation are gone. All manageable by hand at this volume.

Use bold labels on their own lines, no colon inside the bold, per the standing instructions.

Then mark the old recommendation superseded in place, in two spots, without deleting either:
the recommendation paragraph in section 1, and the section 3 heading plus a one-line note
directly beneath it pointing back at section 0. Everything else stays whole. The tier
comparison, the tables, the AUP quotes, and the sources are the evidence trail for why no
provider was chosen. Do not delete or shorten any of it.

The Key Decisions table in PROJECT.md is Task 4's job, not this one. Leave it alone here.
  </action>
  <verify>
    <automated>cd "/Users/annan/Developer/Claude code/projects/website-scanner" && grep -q 'Non-negotiable' .planning/PROJECT.md && grep -q 'Non-negotiable' .claude/CLAUDE.md && grep -c 'deliberately open' .planning/PROJECT.md | grep -qx 0 && grep -c 'deliberately UNDECIDED' .planning/PROJECT.md | grep -qx 0 && grep -q '^## 0\.' .planning/research/SEND-CHANNEL.md && grep -q '^## 2\. The Three Tiers' .planning/research/SEND-CHANNEL.md && grep -q '^## 6\. Sources' .planning/research/SEND-CHANNEL.md && grep -ci 'supersed' .planning/research/SEND-CHANNEL.md | grep -qvx 0 && test "$(grep -c 'Tier 3' .planning/research/SEND-CHANNEL.md)" -ge 1 && test "$(wc -l < .planning/research/SEND-CHANNEL.md)" -gt 130 && echo PASS</automated>
  </verify>
  <done>
Both PROJECT.md constraints match the new SND-02 and record the provider removal, with
"Non-negotiable" intact. The CLAUDE.md copy is word-for-word identical to the PROJECT.md
version. No claim of an open or undecided channel survives in PROJECT.md. SEND-CHANNEL.md
opens with a section 0 recording the decision, sections 1 through 6 are all still present,
the file is longer than it started, and the old recommendation is marked superseded in both
places rather than removed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add the manual-send row to the PROJECT.md Key Decisions table</name>
  <files>.planning/PROJECT.md</files>
  <action>
The Key Decisions table starts at line 218. Its first row records that Resend is ruled out
for outreach. The decision that superseded the open-channel position which followed from
that row is missing entirely. A decision table that omits the decision overturning the row
above it is worse than no table.

Add exactly one row, inserted directly after the Resend row so the two read together.

**Match the existing structure exactly. Do not restructure the table.** Three columns,
`Decision | Rationale | Outcome`, in that order. The header row and separator row stay
untouched. Do not add a Date column. The date goes inside the Decision cell, which is how
the Resend row already carries its own dating and supersession note.

Row content, in the table's existing style, with the decision name bolded and any
parenthetical outside the bold:

- **Decision cell.** Outreach is sent manually from Joshua's own mailbox, with no
  third-party dispatch provider. Carry the date 2026-08-04 in a parenthetical, and note
  that it supersedes the open-channel position.
- **Rationale cell.** Two sentences, no more. First: at 10 to 50 sends per week the
  acceptable-use problem is removed rather than answered, and the Phase 2 suppression spine
  plus the Phase 6 human gate stay intact. Second: every transactional ESP evaluated
  (Mailgun, SendGrid, Brevo, Amazon SES, Resend) prohibits the activity outright, so
  switching providers was never a fix. Then the reference to
  `.planning/research/SEND-CHANNEL.md`.
- **Outcome cell.** Follow the existing vocabulary in that column, which is a ticked "Good"
  with a short parenthetical, or "Pending". This one is decided and closed the provider half
  of the Phase 8 gate, so it takes the ticked form, not Pending.

Two style traps in this table. Some existing Outcome cells use an em-dash before "Pending".
That is existing text and stays as it is, but do not put an em-dash in the cells you write.
And keep the bold decision name free of any colon inside the bold, on the single table line,
per the standing instructions.

Change nothing else in the table. No other row is edited, reordered, or removed.
  </action>
  <verify>
    <automated>cd "/Users/annan/Developer/Claude code/projects/website-scanner" && BLOCK=$(awk '/^## Key Decisions/,/^## Evolution/' .planning/PROJECT.md) && test "$(printf '%s\n' "$BLOCK" | grep -c '^| ')" -eq 16 && NEWROW=$(printf '%s\n' "$BLOCK" | grep -A1 'Resend is ruled out for outreach' | tail -1) && printf '%s\n' "$NEWROW" | grep -qi 'manually' && printf '%s\n' "$NEWROW" | grep -q '2026-08-04' && printf '%s\n' "$NEWROW" | grep -q 'SEND-CHANNEL.md' && printf '%s\n' "$NEWROW" | grep -qi 'Mailgun' && test "$(printf '%s\n' "$NEWROW" | awk -F'|' '{print NF}')" -eq 5 && printf '%s\n' "$NEWROW" | grep -c '—' | grep -qx 0 && grep -c '^- \[x\]' .planning/REQUIREMENTS.md | grep -qx 58 && grep -c '^- \[ \]' .planning/REQUIREMENTS.md | grep -qx 9 && echo PASS</automated>
  </verify>
  <done>
The Key Decisions block holds 16 lines starting with a pipe and a space, being the header
plus 15 data rows, one more than the 14 it started with. The line immediately following the
Resend row is the new one, and it carries the manual-send decision, the date 2026-08-04, the
ESP list, and the SEND-CHANNEL.md reference. It has exactly three columns, matching every
other row, and contains no em-dash. REQUIREMENTS.md checkbox counts are still 58 and 9,
proving this task stayed inside PROJECT.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning record to future executor | The next agent to plan or build Phase 8 reads these files as fact and builds against them. A wrong statement here becomes wrong code later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-n18-01 | Tampering | The Phase 8 gate record in REQUIREMENTS.md and ROADMAP.md | high | mitigate | Only half the gate closed. Every edit that touches the gate must name the legal half as still open. Task 1 and Task 2 both carry a verify gate that the phase stays blocked and the Phase 8 checkbox stays unticked. |
| T-n18-02 | Repudiation | SND-04 written verification | medium | mitigate | SEND-CHANNEL.md is the only evidence artifact for why no provider was chosen. Task 3's verify gate proves sections 1 through 6 survive and the file grew rather than shrank. |
| T-n18-03 | Information disclosure | Contradictory auto-loaded constraint in .claude/CLAUDE.md | medium | mitigate | The mirrored constraint is updated in the same task as its PROJECT.md original, so no session loads a contradicted rule. |
| T-n18-SC | Tampering | Package installs | low | accept | No package manager runs in this plan. Documentation only, no dependency surface. |
</threat_model>

<verification>

Run after all four tasks.

**Nothing but documentation changed.** This is the hard gate on the whole plan.

```
cd "/Users/annan/Developer/Claude code/projects/website-scanner"
git status --porcelain | grep -cE '\.(ts|tsx|sql|json|mjs|js|css)$'
```

Must return `0`. The repo started clean apart from the untracked `SEND-CHANNEL.md`, so the
changed set must be exactly these five paths plus this plan directory:

```
git status --porcelain
```

Expect only: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/PROJECT.md`,
`.claude/CLAUDE.md`, `.planning/research/SEND-CHANNEL.md`, and files under
`.planning/quick/260804-n18-record-manual-send-decision-for-phase-8-/`. Anything else means
the task went out of bounds and must be reverted.

**The three copies of SND-02 agree.** Read the SND-02 line in REQUIREMENTS.md, the Legal
constraint in PROJECT.md, and success criterion 2 in ROADMAP.md side by side. All three must
describe the same opt-out. If they differ, REQUIREMENTS.md wins and the other two get
corrected.

**Requirement IDs survived.** Re-run this at the very end, after Task 4. The Key Decisions
table lives in PROJECT.md and must not have moved these counts.

```
grep -c '\*\*SND-0' .planning/REQUIREMENTS.md        # 4
grep -c '^- \[x\]' .planning/REQUIREMENTS.md          # 58
grep -c '^- \[ \]' .planning/REQUIREMENTS.md          # 9
grep -c 'CMP-02\|CMP-09\|CMP-11\|CMP-12' .planning/ROADMAP.md   # non-zero
```

**The decision table records the decision.** Read the Key Decisions table top to bottom. The
Resend row and the manual-send row must read as a pair, in that order, and a reader who
knows nothing about this project should be able to follow why the channel question opened
and how it closed.

**No em-dash in newly written prose.** Check the diff, not the files, since existing text
keeps its punctuation:

```
git diff -U0 -- .planning .claude | grep '^+' | grep -c '—'
```

Existing lines quoted back verbatim, for example the Phase 8 heading, are the only allowed
hits. Anything in a sentence you authored gets rewritten.

</verification>

<success_criteria>

- SND-02 states a fast, free, one-step opt-out, with the RFC 8058 rationale recorded beside it
- The Send gate note says the provider half closed 2026-08-04 and the legal half still gates Phase 8
- SND-01 and SND-04 are annotated, unticked, and SEND-CHANNEL.md is named as the SND-04 artifact
- Both PROJECT.md constraints are updated, "Non-negotiable" is intact, and the CLAUDE.md mirror matches word for word
- ROADMAP criteria 1 and 2, the blocked banner, the four new notes, the Parallel Track Status column, and the Progress status cell are all in place
- SEND-CHANNEL.md opens with the decision, marks its recommendation superseded in two places, and keeps the tier comparison whole
- The PROJECT.md Key Decisions table carries the manual-send row, directly below the Resend row, in the table's existing three-column shape
- Phase 8 remains unstarted, unticked, and blocked
- `git status --porcelain` shows five documentation files and nothing else

</success_criteria>

<output>

Create `.planning/quick/260804-n18-record-manual-send-decision-for-phase-8-/260804-n18-SUMMARY.md`
when done. Record in it:

- The manual-send decision and its date, so the summary stands alone as the change record
- Any correct-the-falsehood instance you found beyond the ones named in the tasks
- Anything you chose not to change and why, so the next reader is not left guessing

Then commit:

```
git add .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/PROJECT.md \
        .claude/CLAUDE.md .planning/research/SEND-CHANNEL.md \
        .planning/quick/260804-n18-record-manual-send-decision-for-phase-8-/
git commit -m "docs(quick): record the manual-send decision for Phase 8"
```

</output>
