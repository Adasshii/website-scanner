---
phase: 03-triage-shortlist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/016_add_scan_release_marker.sql
  - types/triage.ts
  - lib/triage-constants.ts
  - tests/fixtures/triage-html.ts
  - tests/fixtures/triage-responses.ts
autonomous: true
requirements: [TRI-08, TRI-09]
must_haves:
  truths:
    - "The prospects table carries a nullable scan_released_at column in the local DB after migrations apply"
    - "A single constants module holds the ceiling (~20), default cutoff (60), fetch limits, and weighted-band thresholds"
    - "Deterministic HTML and redirect-response fixtures exist for the Wave 0 signal-extraction and scorer tests"
  artifacts:
    - supabase/migrations/016_add_scan_release_marker.sql
    - types/triage.ts
    - lib/triage-constants.ts
    - tests/fixtures/triage-html.ts
    - tests/fixtures/triage-responses.ts
  key_links:
    - "types/triage.ts TriageSignals/TriageScore are the contract every downstream triage module imports"
    - "lib/triage-constants.ts RELEASE_CEILING + DEFAULT_CUTOFF are the single source for TRI-08/TRI-09 tuning"
---

<objective>
Lay the browserless-triage foundations: the release-marker migration (016), the shared
TriageSignals/TriageScore type contract, the single tunable constants block (ceiling,
cutoff, fetch limits, weighted-band thresholds), and the Wave 0 deterministic test
fixtures every later plan's tests depend on.

Purpose: every other Phase 3 plan imports these. Defining the interface and constants
first (interface-first ordering) lets the compute, release, and UI plans build against a
fixed contract instead of inventing one each. The migration adds the one new column
(`scan_released_at`) that is the entire Phase 3 → Phase 4 handoff (D-08).
Output: migration 016, `types/triage.ts`, `lib/triage-constants.ts`, and two fixture
modules.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-triage-shortlist/03-CONTEXT.md
@.planning/phases/03-triage-shortlist/03-RESEARCH.md
@.planning/phases/03-triage-shortlist/03-PATTERNS.md
</context>

## Artifacts This Phase Produces

New symbols/files created across Phase 3 (for drift-verification exclusion). This plan
creates the ones marked ★:

- ★ `supabase/migrations/016_add_scan_release_marker.sql`; DB column `prospects.scan_released_at`; index `idx_prospects_scan_released_at_null`
- ★ `types/triage.ts` — interfaces `TriageSignals`, `TriageScore`
- ★ `lib/triage-constants.ts` — `TRIAGE_USER_AGENT`, `MAX_HOPS`, `HOP_TIMEOUT_MS`, `MAX_BODY_BYTES`, `BATCH_SIZE`, `BATCH_DELAY_MS`, `DEFAULT_CUTOFF`, `RELEASE_CEILING`, `TARGET_PASS_RATE`, weighted-band thresholds/deductions
- ★ `tests/fixtures/triage-html.ts`, `tests/fixtures/triage-responses.ts`
- `lib/triage-fetch.ts` (`fetchTriageSignals`, `readBodyCapped`, `isHomepageDisallowed`, `parseRobotsForRoot`, `VIEWPORT_RE`, `TriageDeps`), `lib/triage-scorer.ts` (`computeTriageScore`)
- `lib/triage-candidates.ts` (`getTriageCandidates`, `getShortlist`), `lib/triage-release.ts` (`selectWorstN`, `releaseWorstN`)
- `scripts/triage-prospects.ts` (npm script `triage`, `TriageArgsError`, `defaultDeps`)
- `app/api/admin/release-prospects/route.ts`, `app/api/admin/shortlist/route.ts`
- `app/admin/page.tsx` `"shortlist"` tab; components `ShortlistTable`, `CutoffSlider`, `SignalChips`, `ReleaseButton`

<tasks>

<task type="auto">
  <name>Task 1: Migration 016 — scan_released_at release marker + partial index</name>
  <files>supabase/migrations/016_add_scan_release_marker.sql</files>
  <read_first>
    - supabase/migrations/010_create_prospects.sql (prospects columns; RLS already enabled; `country text not null` — the Pitfall-3 column)
    - supabase/migrations/014_create_suppressions.sql (partial-index + `create ... if not exists` convention to mirror exactly)
    - .planning/phases/03-triage-shortlist/03-RESEARCH.md §"Migration 016 (release marker)"
  </read_first>
  <action>
    Create migration `016_add_scan_release_marker.sql` adding one nullable column and one
    partial index to the existing `prospects` table (D-08 — the single Phase 3 → Phase 4
    state change). Use `alter table prospects add column if not exists scan_released_at
    timestamptz;`. Add `create index if not exists idx_prospects_scan_released_at_null on
    prospects (scan_released_at) where scan_released_at is null;` to speed the "not yet
    released" eligibility filter, mirroring the partial-index convention in migration 014.
    Do NOT re-enable RLS (already enabled in 010) and do NOT add a policy (service-role-only
    convention). NULL means not-yet-released; a set timestamp means released and permanently
    excluded from future release (D-06) and future re-triage (D-09). Add a header comment
    block matching the style of migrations 010/014 explaining the column's role. Apply it to
    the local Supabase stack for downstream integration tests via `supabase db reset` (the
    established local-apply command — see the PRECONDITION comment in
    lib/prospect-upsert.integration.test.ts). Do NOT push to production here — that is the
    human-gated blocking task in Plan 06.
  </action>
  <verify>
    <automated>test -f supabase/migrations/016_add_scan_release_marker.sql && grep -q 'scan_released_at timestamptz' supabase/migrations/016_add_scan_release_marker.sql && grep -q 'idx_prospects_scan_released_at_null' supabase/migrations/016_add_scan_release_marker.sql && grep -q 'add column if not exists' supabase/migrations/016_add_scan_release_marker.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/016_add_scan_release_marker.sql` exists and is numbered 016 (next after 015).
    - Contains `add column if not exists scan_released_at timestamptz`.
    - Contains a partial index `idx_prospects_scan_released_at_null ... where scan_released_at is null`.
    - Contains NO `alter table prospects enable row level security` (not re-enabled) and NO `create policy`.
    - Local apply proof: `supabase db reset` completes, then `select scan_released_at from prospects limit 0;` against `http://127.0.0.1:54321` returns without a "column does not exist" error.
  </acceptance_criteria>
  <done>Migration 016 adds the nullable release marker + partial index locally; production push deferred to Plan 06.</done>
</task>

<task type="auto">
  <name>Task 2: Shared triage types + tunable constants block</name>
  <files>types/triage.ts, lib/triage-constants.ts</files>
  <read_first>
    - .planning/phases/03-triage-shortlist/03-RESEARCH.md §"Proposed triage_score jsonb shape" and §"Proposed default weighted-score bands"
    - lib/scoring.ts (existing 0-100 direction convention the triage score mirrors — read, do NOT import)
    - .planning/phases/03-triage-shortlist/03-CONTEXT.md D-01..D-06 (gate, weighted band, ceiling ~20, cutoff, ~30% pass-rate)
  </read_first>
  <action>
    Create `types/triage.ts` exporting two interfaces (this is the D-02 storage contract —
    `prospects.triage_score` jsonb holds the numeric score AND every raw signal). `TriageSignals`
    = the raw fetch output: `reachable: boolean`, `https: boolean`, `finalStatus: number | null`,
    `redirectChain: Array<{ url: string; status: number }>`, `hasViewport: boolean`,
    `bytes: number | null`, `truncated: boolean`, `responseMs: number | null`,
    `robotsBlocked: boolean`, and `gateReason: "unreachable" | "no-https" | null`.
    `TriageScore` = `TriageSignals` plus `score: number` (0-100, LOWER = worse — mirrors
    lib/scoring.ts direction) and `gated: boolean`. These are the contract every downstream
    triage module imports; do not redefine them elsewhere.

    Create `lib/triage-constants.ts` as the single tunable block (D-03/D-04 "documented in
    one place"). Export: `TRIAGE_USER_AGENT` (honest identifiable UA naming the tool + a
    contact URL per D-12, e.g. an "AdashiTriage/1.0 (+https://...)" style string — NOT a
    browser UA and NOT the scanner's "AdashiScanner/1.0"); `MAX_HOPS = 8`;
    `HOP_TIMEOUT_MS = 5000`; `MAX_BODY_BYTES = 5 * 1024 * 1024`; `BATCH_SIZE = 5`;
    `BATCH_DELAY_MS = 500`; `DEFAULT_CUTOFF = 60`; `RELEASE_CEILING = 20`;
    `TARGET_PASS_RATE = 0.30`. Export the weighted-band thresholds and deductions as named
    constants (not magic numbers inline in the scorer): viewport-missing deduction 30;
    redirect-hops thresholds at 4 (deduct 25) and 2 (deduct 15); page-weight thresholds at
    3_000_000 bytes (deduct 20) and 1_000_000 bytes (deduct 10); response-time thresholds at
    4000ms (deduct 20) and 1500ms (deduct 10). Add a one-line comment marking every
    threshold/deduction as a tunable default (Claude's discretion per D-03), and a comment
    stating the score direction (lower = worse) and the D-01 gate rule.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `types/triage.ts` exports `TriageSignals` and `TriageScore`; `TriageScore` includes `score: number` and `gated: boolean`.
    - `lib/triage-constants.ts` exports `RELEASE_CEILING` (=20), `DEFAULT_CUTOFF` (=60), `MAX_HOPS`, `HOP_TIMEOUT_MS`, `MAX_BODY_BYTES`, `BATCH_SIZE`, `BATCH_DELAY_MS`, `TRIAGE_USER_AGENT`.
    - `TRIAGE_USER_AGENT` is not equal to the string `AdashiScanner/1.0` (distinct honest UA per D-12): `grep -q 'AdashiScanner/1.0' lib/triage-constants.ts` returns no match.
    - Every weighted-band threshold/deduction from the research table is a named export, not an inline literal reused by the scorer.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The type contract and the single tunable constants block compile and are importable by all later plans.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 deterministic fixtures — HTML variants + redirect-response sequences</name>
  <files>tests/fixtures/triage-html.ts, tests/fixtures/triage-responses.ts</files>
  <read_first>
    - tests/fixtures/overture.ts (the "sane, overridable defaults" builder convention to mirror)
    - .planning/phases/03-triage-shortlist/03-RESEARCH.md §"Wave 0 Gaps" (exact fixture cases required) and §"Validation Architecture"
    - .planning/phases/03-triage-shortlist/03-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <action>
    Create `tests/fixtures/triage-html.ts` exporting canned raw-HTML strings covering the
    viewport-regex cases the TRI-04 tests assert against: (a) no viewport meta at all,
    (b) `name="viewport"` with double quotes, (c) single quotes, (d) `content=` attribute
    appearing BEFORE `name=` in the tag, (e) an unquoted `name=viewport` value, (f) a
    "JS-injected only" page whose raw HTML has NO viewport tag (the SPA false-negative case).
    Follow the overture.ts builder shape (named exports / a small factory with overridable
    defaults), not a parsed DOM — raw strings only (the phase rejects DOM libraries even in
    tests).

    Create `tests/fixtures/triage-responses.ts` exporting fake fetch-response sequences for
    the DI seam in `lib/triage-fetch.ts`: a clean 200; an `http://` → `https://` single-hop
    upgrade; a redirect chain exceeding MAX_HOPS (loop); a 500 final status (reachable but
    broken); a DNS-failure throw (an Error whose `cause.code` is `ENOTFOUND`); a redirect
    whose `Location` points at a private/metadata IP (`http://169.254.169.254/`) for the
    per-hop SSRF test; and an oversized (>5MB) body to exercise the truncation path. Each
    fixture should be a value or factory a test can hand to a stubbed `fetch`, mirroring how
    import-prospects.test.ts stubs its deps.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/triage-html.ts` exports at least the 6 viewport cases listed (no-viewport, double-quote, single-quote, content-before-name, unquoted, JS-injected-absent).
    - `tests/fixtures/triage-responses.ts` exports fixtures including an `http→https` upgrade, a >MAX_HOPS loop, a 500 final status, an `ENOTFOUND` throw, a `169.254.169.254` redirect target, and a >5MB body.
    - Both files import types from `@/types/triage` where they describe signal shapes and compile under `npx tsc --noEmit`.
    - No import of jsdom, cheerio, or any DOM parser: `grep -rEn "from ['\"](jsdom|cheerio)" tests/fixtures/triage-*.ts` returns no matches.
  </acceptance_criteria>
  <done>Deterministic fixtures exist for every TRI-02..06 and SSRF-per-hop test the later plans write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live schema | DDL changes the shared prospects table; a wrong migration could corrupt Phase 1 data |
| constants → all consumers | RELEASE_CEILING/DEFAULT_CUTOFF are the only guardrail values; a bad default silently changes budget behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | migration 016 | medium | mitigate | Additive-only (`add column if not exists`, nullable, no data backfill, no drop/alter of existing columns); reversible by dropping the new column; local apply verified before the human prod push in Plan 06 |
| T-03-02 | Denial of Service | lib/triage-constants.ts | high | mitigate | `MAX_BODY_BYTES`, `MAX_HOPS`, `HOP_TIMEOUT_MS`, `BATCH_SIZE`, `BATCH_DELAY_MS`, `RELEASE_CEILING` are all defined here as hard caps; the fetch/release plans consume these constants rather than inline literals, so the caps are single-sourced and cannot silently drift |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase (RESEARCH §Package Legitimacy Audit — zero new deps); nothing to verify |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with the new type + constants + fixture modules.
- `supabase db reset` applies migration 016 to the local stack and `scan_released_at` is queryable.
- `grep` gates in the acceptance criteria pass (honest UA, no DOM libs, named constants).
</verification>

<success_criteria>
- Migration 016 exists, is additive-only, and applies cleanly locally.
- `types/triage.ts` and `lib/triage-constants.ts` compile and export the full contract.
- Wave 0 fixtures cover every viewport/redirect/SSRF/truncation case the later tests need.
</success_criteria>

<output>
Create `.planning/phases/03-triage-shortlist/03-01-SUMMARY.md` when done.
</output>
