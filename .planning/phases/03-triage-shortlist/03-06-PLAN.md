---
phase: 03-triage-shortlist
plan: 06
type: execute
wave: 5
depends_on: ["03-05"]
files_modified: []
autonomous: false
requirements: [TRI-08, TRI-09]
user_setup:
  - service: supabase
    why: "Apply migration 016 (scan_released_at release marker) to the live project — human-gated by project convention (migrations 010–015 were all pushed manually via the dashboard SQL Editor)"
    dashboard_config:
      - task: "Run supabase/migrations/016_add_scan_release_marker.sql in the SQL Editor"
        location: "Supabase Dashboard → SQL Editor → New query (live project)"
must_haves:
  truths:
    - "The live prospects table has the scan_released_at column and its partial index before any real triage/release runs against production"
  artifacts: []
  key_links:
    - "Without the live column, the shortlist GET, release route, and getTriageCandidates all query a non-existent column and fail against production — while build/typecheck falsely pass"
---

<objective>
Apply migration 016 (`scan_released_at` + `idx_prospects_scan_released_at_null`) to the LIVE
Supabase project. This is the mandatory human-gated schema push: by project convention
(STATE.md Phase 01-01 decision; RESEARCH §Environment Availability) migrations to the live
DB are applied manually via the Supabase dashboard SQL Editor, the same way migrations
010–015 were — never via an automated `supabase db push` against production.

Purpose: build, typecheck, and lint all pass WITHOUT this column (types come from
`types/triage.ts` and the migration file, not the live DB), which creates a false-positive
"green" state. The shortlist GET route, the release route, and `getTriageCandidates` all
filter on `scan_released_at`; against production without the column they fail at runtime.
This plan closes that gap immediately before end-to-end phase verification.
Output: the `scan_released_at` column live in production. No repo files change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@supabase/migrations/016_add_scan_release_marker.sql
</context>

## Artifacts This Phase Produces

This plan creates no new symbols. It applies the already-written migration
`supabase/migrations/016_add_scan_release_marker.sql` (column `prospects.scan_released_at`,
index `idx_prospects_scan_released_at_null`) to the live database. The full Phase 3 artifact
list is enumerated in 03-01-PLAN.md.

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Apply migration 016 to the live Supabase project (human-gated)</name>
  <files>supabase/migrations/016_add_scan_release_marker.sql (applied, not modified)</files>
  <read_first>
    - supabase/migrations/016_add_scan_release_marker.sql (the exact DDL to paste)
    - .planning/STATE.md (Phase 01-01 human-gated live-migration decision)
  </read_first>
  <what-built>
    Migration `supabase/migrations/016_add_scan_release_marker.sql` (written and locally
    verified in Plan 01) adds the nullable `scan_released_at timestamptz` column and the
    partial index `idx_prospects_scan_released_at_null` to the `prospects` table. It is
    additive-only, reversible, and touches no existing column or data.
  </what-built>
  <action>
    HUMAN ACTION (Joshua) — do NOT run an automated `supabase db push` against production; the
    dashboard SQL Editor is the established manual-push path for this project (migrations
    010–015 were all applied this way).
    1. Open the Supabase Dashboard for the LIVE Prospect Radar project → SQL Editor → New query.
    2. Paste the full contents of `supabase/migrations/016_add_scan_release_marker.sql` and Run.
       The statements use `if not exists`, so re-running is safe.
    3. Confirm the column and index exist using the two verification queries below.
  </action>
  <verify>
    <human-check>
      Run in the live SQL Editor and confirm each returns one row:
      (a) `select column_name, data_type from information_schema.columns where table_name = 'prospects' and column_name = 'scan_released_at';` → expect `scan_released_at | timestamp with time zone`.
      (b) `select indexname from pg_indexes where tablename = 'prospects' and indexname = 'idx_prospects_scan_released_at_null';` → expect one row.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `information_schema.columns` returns one row for `prospects.scan_released_at` (timestamptz) in the live project.
    - `pg_indexes` returns one row for `idx_prospects_scan_released_at_null` on `prospects` in the live project.
    - No automated `supabase db push` was run against production (dashboard SQL Editor only).
  </acceptance_criteria>
  <done>The live prospects table carries scan_released_at + its partial index; end-to-end triage/release against production is unblocked.</done>
  <resume-signal>Reply "applied" once both queries return their expected rows, or paste the error if the migration failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human operator → live database | Manual DDL against the production prospects table shared with the live Phase 1 data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-prod-migrate | Tampering / data integrity | live prospects table | medium | mitigate | Additive-only, `if not exists`, nullable, no backfill, no alter/drop of existing columns → zero risk to existing rows; human reviews and runs it, and verifies the column + index with the two provided queries before resuming |
| T-03-falsepos | (verification integrity) | build/typecheck | high | mitigate | This blocking human gate is the control: it forces the live column into existence before end-to-end phase verification, so the false-positive green build state cannot let the phase pass without the column |
| T-03-SC | Tampering | npm installs | high | mitigate | No installs; no code changes in this plan |
</threat_model>

<verification>
- `information_schema.columns` shows `prospects.scan_released_at` (timestamptz) in the live project.
- `pg_indexes` shows `idx_prospects_scan_released_at_null` in the live project.
- After this gate: `npm run triage -- --dry-run --limit 5` against production candidates runs without a "column does not exist" error, and the admin Shortlist/Release flow works end-to-end.
</verification>

<success_criteria>
- The live `prospects` table carries `scan_released_at` and its partial index.
- End-to-end triage + release against production is unblocked and phase verification can proceed.
</success_criteria>

<output>
Create `.planning/phases/03-triage-shortlist/03-06-SUMMARY.md` when done.
</output>
