---
phase: 01-prospect-data-foundation-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/010_create_prospects.sql
  - supabase/migrations/011_create_prospect_sources.sql
  - supabase/migrations/012_create_outreach_messages.sql
  - supabase/migrations/013_add_prospect_id_to_scans.sql
autonomous: false
requirements: [IMP-05, IMP-06, IMP-07]
must_haves:
  truths:
    - "A prospects row can exist with domain NULL and lifecycle_state='no_website' (IMP-07)"
    - "Two no-website rows (domain NULL) coexist without a unique-constraint collision (partial index)"
    - "Every prospects row carries a NOT NULL country (IMP-06)"
    - "Work columns (lifecycle_state, triage_score, contact_email, website_url_pending, country_pending) exist so re-import can freeze them by omission (IMP-05)"
    - "scans.prospect_id nullable FK exists; existing inbound-flow scans stay valid with NULL"
  artifacts:
    - supabase/migrations/010_create_prospects.sql
    - supabase/migrations/011_create_prospect_sources.sql
    - supabase/migrations/012_create_outreach_messages.sql
    - supabase/migrations/013_add_prospect_id_to_scans.sql
  key_links:
    - "prospects.id <- prospect_sources.prospect_id (ON DELETE CASCADE)"
    - "prospects.domain partial-unique index WHERE domain IS NOT NULL"
    - "scans.prospect_id -> prospects.id (nullable); prospects.latest_scan_id -> scans.id (nullable)"
---

<objective>
Land the durable data-model foundation for the whole milestone: the `prospects` table
with its domain-or-GERS dual identity, the `prospect_sources` child table, the
`outreach_messages` foundation table, and the `scans.prospect_id` link. Then push the
additive schema to the live production Supabase behind an explicit human gate.

Purpose: Every later plan and phase writes against these tables. The partial-unique
domain index (D-06), the freeze columns (D-04/D-05/D-13), and the `no_website` state
(D-07) are structural — they make IMP-04/IMP-05/IMP-07 true by construction, not by
convention.
Output: Migrations 010–013 applied to production; the DB shape the importer and dedupe
engine (plans 01-03, 01-04) rely on.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prospect-data-foundation-import/01-CONTEXT.md
@.planning/phases/01-prospect-data-foundation-import/01-RESEARCH.md
@.planning/phases/01-prospect-data-foundation-import/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prospects, prospect_sources, and outreach_messages migrations (010–012)</name>
  <files>supabase/migrations/010_create_prospects.sql, supabase/migrations/011_create_prospect_sources.sql, supabase/migrations/012_create_outreach_messages.sql</files>
  <read_first>
    - .planning/phases/01-prospect-data-foundation-import/01-RESEARCH.md (§Code Examples, migration sketches lines 428–524 — the authoritative column list)
    - .planning/phases/01-prospect-data-foundation-import/01-CONTEXT.md (D-01..D-07, D-13, D-14 — column semantics and freeze rules)
    - .planning/phases/01-prospect-data-foundation-import/01-PATTERNS.md (§migration style — lowercase, `if not exists`, RLS on every table; numbering 010–013)
    - supabase/migrations/001_create_scans_and_leads.sql (the exact style analog to copy)
  </read_first>
  <action>
    Create three migration files following the RESEARCH §Code Examples sketches and the
    001 style analog (guarded `create table if not exists`, `idx_<table>_<cols>` index
    names, one-line purpose comment above each table, and `alter table <name> enable row
    level security;` at the end of EACH new table — do not skip RLS for any of the three).

    010_create_prospects.sql — table `prospects` with columns per RESEARCH lines 432–456
    PLUS the two D-13 country-freeze columns the research sketch predates: add
    `country_pending text` and `country_changed_at timestamptz` alongside the existing
    `website_url_pending text` / `website_url_changed_at timestamptz` pair. Keep
    `country text NOT NULL` (IMP-06, D-12 — recorded per prospect, a parameter). Keep `lifecycle_state` with its CHECK list including
    `'no_website'` (D-07). Keep the work columns `triage_score jsonb`,
    `triage_checked_at timestamptz`, `latest_scan_id uuid` (no FK yet — added in 013 to
    avoid a forward reference), `contact_email text`, `contact_email_type text`. Create the
    partial-unique index `prospects_domain_unique_idx ON prospects (domain) WHERE domain IS
    NOT NULL` (D-06 — this is the entire mechanism that lets many NULL-domain no-website
    prospects coexist while has-domain prospects stay unique). Together the `no_website`
    state and this partial index establish the precondition for the D-08 send-gate outreach
    exclusion (whose hard assertion lives in the later outreach phase, not here). Add
    `idx_prospects_lifecycle_state` and `idx_prospects_country`.

    011_create_prospect_sources.sql — table `prospect_sources` (D-02: GERS IDs live in this
    child table, one prospect → many Overture source rows) per RESEARCH lines 470–489:
    `prospect_id uuid NOT NULL REFERENCES prospects(id) ON DELETE CASCADE`,
    `overture_gers_id text NOT NULL UNIQUE` (IMP-03 idempotency key), `overture_release text`,
    the `raw_*` columns, `first_seen_at`/`last_seen_at`/`created_at`, and
    `idx_prospect_sources_prospect_id`.

    012_create_outreach_messages.sql — table `outreach_messages` per RESEARCH lines 494–511
    (foundation only; behavior lands in Phase 6). Declare `scan_id uuid REFERENCES scans(id)`
    inline (nullable) since `scans` already exists in this DB. Keep the `status` CHECK list
    and `idx_outreach_messages_prospect_id`.

    Do NOT populate any PII: `contact_email`/`contact_email_type` are created nullable and
    stay empty this phase (Phase 5 writes them). Use lowercase SQL keywords to match 001.
  </action>
  <verify>
    <automated>test -f supabase/migrations/010_create_prospects.sql && test -f supabase/migrations/011_create_prospect_sources.sql && test -f supabase/migrations/012_create_outreach_messages.sql && grep -iq 'where domain is not null' supabase/migrations/010_create_prospects.sql && grep -iq 'country_pending' supabase/migrations/010_create_prospects.sql && grep -ciq 'enable row level security' supabase/migrations/010_create_prospects.sql supabase/migrations/011_create_prospect_sources.sql supabase/migrations/012_create_outreach_messages.sql</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under supabase/migrations/.
    - 010 contains a partial-unique index whose predicate matches `WHERE domain IS NOT NULL` (case-insensitive).
    - 010 contains `country text` declared NOT NULL, plus columns `country_pending`, `country_changed_at`, `website_url_pending`, `website_url_changed_at`, `lifecycle_state`, `triage_score`, `contact_email`, `contact_email_type`, `latest_scan_id`.
    - 010's `lifecycle_state` CHECK constraint list includes `'no_website'`.
    - 011 declares `overture_gers_id text NOT NULL UNIQUE` and `prospect_id ... REFERENCES prospects(id) ON DELETE CASCADE`.
    - Each of the three files ends with an `enable row level security` statement for its table.
  </acceptance_criteria>
  <done>prospects, prospect_sources, and outreach_messages migration files exist, RLS-enabled, matching the RESEARCH schema plus the D-13 country-pending columns.</done>
</task>

<task type="auto">
  <name>Task 2: Create scans.prospect_id + latest_scan_id FK migration (013)</name>
  <files>supabase/migrations/013_add_prospect_id_to_scans.sql</files>
  <read_first>
    - .planning/phases/01-prospect-data-foundation-import/01-RESEARCH.md (§Code Examples lines 514–524 — the 013 sketch and the ordering note about latest_scan_id's FK)
    - supabase/migrations/009_bilingual_ai_content.sql (the additive-ALTER analog: `ADD COLUMN IF NOT EXISTS` + a "why nullable / what legacy rows do" comment block)
  </read_first>
  <action>
    Create 013_add_prospect_id_to_scans.sql mirroring 009's additive-ALTER style. Open with
    a comment block (009's tone) explaining that `prospect_id` is a nullable FK: existing
    inbound-flow scans leave it NULL, and the Phase 4 dispatcher sets it for prospect scans.
    Statements: `ALTER TABLE scans ADD COLUMN IF NOT EXISTS prospect_id uuid REFERENCES
    prospects(id);` then a partial index `idx_scans_prospect_id ON scans (prospect_id) WHERE
    prospect_id IS NOT NULL`. Then add the reciprocal FK now that both tables exist:
    `ALTER TABLE prospects ADD CONSTRAINT prospects_latest_scan_id_fkey FOREIGN KEY
    (latest_scan_id) REFERENCES scans(id);`. This constraint lives in 013, not 010, to avoid
    the forward-reference ordering problem. All statements idempotent/re-runnable.
  </action>
  <verify>
    <automated>test -f supabase/migrations/013_add_prospect_id_to_scans.sql && grep -q 'add column if not exists prospect_id' <(tr 'A-Z' 'a-z' < supabase/migrations/013_add_prospect_id_to_scans.sql) && grep -q 'prospects_latest_scan_id_fkey' supabase/migrations/013_add_prospect_id_to_scans.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists and adds `prospect_id uuid REFERENCES prospects(id)` to scans via `ADD COLUMN IF NOT EXISTS`.
    - File adds a `prospect_id` index guarded by `WHERE prospect_id IS NOT NULL`.
    - File adds the `prospects_latest_scan_id_fkey` FK from prospects.latest_scan_id to scans(id).
    - A leading comment block explains why prospect_id is nullable (existing inbound scans stay NULL).
  </acceptance_criteria>
  <done>scans.prospect_id and prospects.latest_scan_id FKs exist as additive, idempotent DDL matching the 009 style.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Push additive schema (010–013) to live production Supabase</name>
  <action>
    HUMAN GATE. After Tasks 1–2 have written migrations 010–013, request explicit
    authorization, then run `supabase db push` (set SUPABASE_ACCESS_TOKEN for non-TTX/non-TTY)
    against the LIVE production Supabase. State the additive-only + idempotent precondition
    (three new tables, one new nullable column scans.prospect_id, two FKs, zero rows touched
    on existing tables, re-run safe) BEFORE the push. Do not proceed autonomously — this is
    the project's per-gate live-DB authorization.
  </action>
  <what-built>
    Migrations 010–013 (three new tables: prospects, prospect_sources, outreach_messages;
    one nullable column scans.prospect_id; two FKs). All DDL is additive and idempotent.
  </what-built>
  <how-to-verify>
    This push mutates the LIVE production Supabase — the same database that serves the
    earning public scanner. The project has no staging DB, so per the blast-radius
    constraint this is an explicit per-gate human authorization, not an autonomous step.

    Precondition check the executor states BEFORE requesting approval:
    1. The migration set is ADDITIVE ONLY — three brand-new tables plus exactly one new
       nullable column (`scans.prospect_id`) and two new FKs. Zero rows on existing tables
       (`scans`, `leads`, `email_events`) are read or written by this DDL.
    2. Every statement is `IF NOT EXISTS` / idempotent, so a re-run is safe.
    3. No existing column is altered, dropped, or renamed; no data is migrated.

    Run: `supabase db push` (non-TTY: ensure `SUPABASE_ACCESS_TOKEN` is set in the
    environment first). If the CLI prompts for confirmation of the migration list, confirm
    only 010–013 are pending.

    After push, verify the tables landed:
    `supabase db execute "SELECT count(*) FROM prospects; SELECT count(*) FROM prospect_sources; SELECT count(*) FROM outreach_messages;"`
    (or the Supabase dashboard Table editor) — all three exist and return 0 rows; a
    `SELECT prospect_id FROM scans LIMIT 1` succeeds (column present, NULL).
  </how-to-verify>
  <resume-signal>Type "approved" once the push succeeded and the three tables + scans.prospect_id column exist on production, or describe the failure.</resume-signal>
  <verify>
    <human-check>On production Supabase: prospects, prospect_sources, outreach_messages exist and return 0 rows; `SELECT prospect_id FROM scans LIMIT 1` succeeds (column present).</human-check>
  </verify>
  <done>Migrations 010–013 are applied to production; all three tables and scans.prospect_id exist; no existing table rows were altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI (service-role key) → live production Supabase | DDL crosses into the DB that also serves the earning public scanner |
| new PII columns created | contact_email / contact_email_type are the GDPR-bearing columns later phases populate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege / Tampering | `supabase db push` against live prod | high | mitigate | Human gate (autonomous:false, blocking-human). Additive-only + idempotent precondition stated before approval; zero rows touched on existing tables; re-run safe. |
| T-01-02 | Information Disclosure | prospects.contact_email / contact_email_type | medium | mitigate | Columns created nullable and left EMPTY this phase (Phase 5 populates). RLS enabled on prospects; service-role-only access; no public read route added. |
| T-01-03 | Repudiation | migration provenance | low | accept | Migrations are versioned files committed to git; standard project convention, no extra control needed at this scale. |
</threat_model>

<verification>
- All four migration files (010–013) exist and lint as valid SQL.
- Partial-unique domain index present on prospects (the D-06 mechanism).
- RLS enabled on all three new tables.
- [BLOCKING] push approved: prospects, prospect_sources, outreach_messages, and
  scans.prospect_id exist on production Supabase.
</verification>

<success_criteria>
- A NULL-domain, `no_website` prospect row is insertable and two of them coexist (partial index).
- Every prospects row requires a non-null country.
- scans.prospect_id accepts NULL (inbound flow) and a valid prospect id.
- Production schema matches the committed migrations 010–013.
</success_criteria>

<artifacts>
## Artifacts this phase produces (full Phase 1 inventory)

Tables: `prospects`, `prospect_sources`, `outreach_messages`
Column added: `scans.prospect_id` (nullable FK)
prospects columns of note: `domain`, `country`, `website_url`, `website_url_pending`,
`website_url_changed_at`, `country_pending`, `country_changed_at`, `lifecycle_state`
(incl. `no_website`), `triage_score`, `triage_checked_at`, `latest_scan_id`,
`contact_email`, `contact_email_type`, `campaign_tag`
Indexes: `prospects_domain_unique_idx` (partial unique WHERE domain IS NOT NULL),
`idx_prospects_lifecycle_state`, `idx_prospects_country`,
`idx_prospect_sources_prospect_id`, `idx_outreach_messages_prospect_id`,
`idx_scans_prospect_id`; FKs `prospects_latest_scan_id_fkey`, scans.prospect_id → prospects.id
Migration files: `supabase/migrations/010_create_prospects.sql`,
`011_create_prospect_sources.sql`, `012_create_outreach_messages.sql`,
`013_add_prospect_id_to_scans.sql`
New deps (plan 01-02): `@duckdb/node-api`, `tldts`, `tsx`, `vitest`
New types (plan 01-02): `OverturePlaceRow`, `ProspectRow`, `ProspectSourceRow`
New functions (plan 01-03): `normalizeDomain`, `DomainValidationError`, `upsertOverturePlace`
New module (plan 01-04): `queryOverturePlaces` (lib/overture-client.ts), `detectCategoryColumn`
New CLI (plan 01-04): `scripts/import-prospects.ts` — flags `--country`, `--region`,
`--category`, `--dry-run`, `--limit`
Fixture generator (plan 01-02): `tests/fixtures/overture.ts`
</artifacts>

<output>
Create `.planning/phases/01-prospect-data-foundation-import/01-01-SUMMARY.md` when done.
</output>
