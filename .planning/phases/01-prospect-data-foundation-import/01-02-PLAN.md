---
phase: 01-prospect-data-foundation-import
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - types/scanner.ts
  - tests/fixtures/overture.ts
autonomous: false
requirements: [IMP-01, IMP-04]
user_setup: []
must_haves:
  truths:
    - "`npx vitest run` executes (framework installed, test script present)"
    - "OverturePlaceRow / ProspectRow / ProspectSourceRow types are importable from types/scanner.ts"
    - "A fixture generator produces synthetic OverturePlaceRow objects with controllable gersId / websiteUrl / country so DB-free and integration tests need no live Overture access"
    - "@duckdb/node-api and tldts are installed only after human verification on npmjs.com"
  artifacts:
    - vitest.config.ts
    - types/scanner.ts
    - tests/fixtures/overture.ts
  key_links:
    - "tests import OverturePlaceRow from types/scanner.ts and build rows via tests/fixtures/overture.ts"
    - "tldts backs normalizeDomain (01-03); @duckdb/node-api backs overture-client (01-04)"
---

<objective>
Stand up the repo's first test capability (vitest — none exists today) and install the
two new runtime dependencies behind the mandated package-legitimacy checkpoint, then add
the shared Overture/prospect types and a synthetic-row fixture generator that every unit
and integration test in this phase depends on.

Purpose: This is the Wave-0 test-infrastructure gap VALIDATION.md flags. Without it, no
`<automated>` verify in plans 01-03/01-04 can run. The types + fixtures let the dedupe
engine be tested without live Overture/DuckDB access.
Output: A runnable vitest, `@duckdb/node-api` + `tldts` + `tsx` installed, the shared
types, and `tests/fixtures/overture.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prospect-data-foundation-import/01-RESEARCH.md
@.planning/phases/01-prospect-data-foundation-import/01-VALIDATION.md
@.planning/phases/01-prospect-data-foundation-import/01-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Verify @duckdb/node-api and tldts legitimacy before install</name>
  <action>
    HUMAN GATE. Before any `npm install` of @duckdb/node-api or tldts, surface both
    packages for human verification on npmjs.com (official org repo, download counts, no
    unexpected postinstall). Both are flagged [SUS]/[ASSUMED] by the recency heuristic and
    are NOT auto-approvable (workflow.auto_advance is ignored here). Do not install until
    approved.
  </action>
  <what-built>
    Nothing yet — this gate precedes the `npm install` of the two new runtime deps.
  </what-built>
  <how-to-verify>
    RESEARCH.md's Package Legitimacy Audit flags both packages [SUS]/[ASSUMED] — solely
    on a "latest publish is too recent" heuristic. Both are almost certainly legitimate
    (official org repos, very high download counts), but per protocol this needs a human
    glance before install. These are NOT auto-approvable.

    Verify each on its registry page:
    1. @duckdb/node-api — https://www.npmjs.com/package/@duckdb/node-api — confirm the
       repository is github.com/duckdb/duckdb-node-neo (official DuckDB org), ~985K weekly
       downloads, no unexpected postinstall beyond native-binary fetch. Target version
       ~1.5.4-r.1.
    2. tldts — https://www.npmjs.com/package/tldts — confirm the repository is
       github.com/remusao/tldts, ~59.5M weekly downloads. Target version ~7.4.9.
  </how-to-verify>
  <resume-signal>Type "approved" to install both, or name any package to reject / substitute.</resume-signal>
  <verify>
    <human-check>Both npmjs.com pages confirm the official org repos (duckdb/duckdb-node-neo, remusao/tldts) and expected download counts.</human-check>
  </verify>
  <done>Both packages are human-verified as legitimate and cleared for install.</done>
</task>

<task type="auto">
  <name>Task 2: Install vitest + tsx + @duckdb/node-api + tldts and add test tooling</name>
  <files>package.json, package-lock.json, vitest.config.ts</files>
  <read_first>
    - package.json (current scripts block: dev/build/start/lint only, no `test` script; no test framework)
    - .planning/phases/01-prospect-data-foundation-import/01-RESEARCH.md (§Standard Stack — exact packages and the install commands; §Validation Architecture — vitest rationale)
    - .planning/phases/01-prospect-data-foundation-import/01-VALIDATION.md (Wave 0: install vitest as root devDependency, add a `test` script)
    - tsconfig.json (the `@/*` -> root path alias the test alias must mirror)
  </read_first>
  <action>
    After Task 1 approval, install the runtime deps: `npm install @duckdb/node-api tldts`.
    Install dev deps: `npm install --save-dev vitest tsx`. Add a `"test": "vitest run"`
    script to root package.json (keep existing dev/build/start/lint untouched). Create a
    minimal `vitest.config.ts` at repo root using the default Node test environment (no
    jsdom — this phase's tests are Node-side logic and DB integration), with `globals` off
    (tests use explicit `import { describe, it, expect } from "vitest"`). Do NOT add any
    watch flag. Alias `@` to the repo root in vitest.config.ts (mirror tsconfig.json's
    `@/*` -> root) so test files can import from `@/lib/...` and `@/types/...`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts.test) throw new Error('no test script'); for (const d of ['vitest','tsx']) if(!p.devDependencies[d]) throw new Error('missing dev '+d); for (const d of ['@duckdb/node-api','tldts']) if(!p.dependencies[d]) throw new Error('missing dep '+d); console.log('deps ok')" && test -f vitest.config.ts && npx vitest run --reporter=basic 2>&1 | grep -Eq 'No test files found|passed|Test Files'</automated>
  </verify>
  <acceptance_criteria>
    - package.json has a `test` script equal to `vitest run` (no watch flag).
    - `@duckdb/node-api` and `tldts` appear under dependencies; `vitest` and `tsx` under devDependencies.
    - vitest.config.ts exists and aliases `@` to the repo root.
    - `npx vitest run` exits cleanly (0 tests is acceptable here — no test files yet).
  </acceptance_criteria>
  <done>vitest runs, the four packages are installed, and `@/*` resolves in tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add Overture/prospect types and the synthetic-row fixture generator</name>
  <files>types/scanner.ts, tests/fixtures/overture.ts</files>
  <read_first>
    - types/scanner.ts (existing ScanRow line 264, LeadRow line 329, PageResult line 53 — mirror the convention: PascalCase interface, inline `/** */` docs on non-obvious fields, `?` with a why-comment for optionals, and match ScanRow's DB-column casing choice)
    - .planning/phases/01-prospect-data-foundation-import/01-RESEARCH.md (§Pattern 1 — the OverturePlaceRow interface at lines 195–204; §Code Examples migration columns for prospect/source field names)
    - .planning/phases/01-prospect-data-foundation-import/01-PATTERNS.md (§types/scanner.ts additions — append to the shared file, do not create types/prospect.ts)
    - supabase/migrations/010_create_prospects.sql (once written by 01-01 — the authoritative column list ProspectRow mirrors)
  </read_first>
  <action>
    Append to types/scanner.ts (PATTERNS.md says default to appending to the shared
    location, not a new types/prospect.ts). Add:
    - `OverturePlaceRow` matching RESEARCH Pattern 1 lines 195–204 exactly: gersId: string,
      name: string | null, address: string | null, category: string | null,
      region: string | null, country: string, websiteUrl: string | null (already reduced
      from websites[0]), confidence: number | null.
    - `ProspectRow` and `ProspectSourceRow` mirroring the 010/011 migration columns. Read
      ScanRow first and match its DB-column casing convention so the file stays consistent.
      ProspectRow must include the D-05 pending pair (website_url_pending /
      website_url_changed_at) and the D-13 pending pair (country_pending /
      country_changed_at), plus lifecycle_state, domain (nullable), country, and the frozen
      work columns.
    Create `tests/fixtures/overture.ts` exporting
    `makeOverturePlace(overrides?: Partial<OverturePlaceRow>): OverturePlaceRow` — sane
    defaults (unique gersId, country "NL", a websiteUrl), fully overridable so a test can
    force websiteUrl null (no-website path), a shared domain across two different gersIds
    (collapse test), or a differing country (D-13 flag test). No live Overture/DuckDB
    dependency.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -q 'OverturePlaceRow' types/scanner.ts && grep -q 'makeOverturePlace' tests/fixtures/overture.ts && grep -Eq 'country_pending|countryPending' types/scanner.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with the new types (no type errors introduced).
    - types/scanner.ts exports OverturePlaceRow, ProspectRow, ProspectSourceRow.
    - OverturePlaceRow fields exactly match RESEARCH Pattern 1 (gersId, name, address, category, region, country, websiteUrl, confidence).
    - ProspectRow includes both pending pairs (website_url and country freeze columns).
    - tests/fixtures/overture.ts exports makeOverturePlace producing a valid OverturePlaceRow, override-friendly.
  </acceptance_criteria>
  <done>Shared types exist and typecheck; a fixture factory produces synthetic Overture rows for DB-free and integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev machine | Two new packages (native bindings for DuckDB) enter the build via `npm install` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs of @duckdb/node-api, tldts | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) before install; verify official org repo + download counts on npmjs.com. Both flagged [SUS]/[ASSUMED] by the recency heuristic; not auto-approvable. |
| T-01-02b | Tampering | @duckdb/node-api native-binary postinstall | medium | mitigate | Checkpoint confirms no postinstall beyond DuckDB's normal native-binary fetch; official DuckDB Foundation package. |
</threat_model>

<verification>
- `npx vitest run` executes (framework live).
- The four packages are present in package.json with the expected dep/devDep placement.
- `npx tsc --noEmit` passes with the new shared types.
- makeOverturePlace produces valid OverturePlaceRow objects.
</verification>

<success_criteria>
- Tests in 01-03/01-04 can import from `@/lib/...`, `@/types/scanner`, and `tests/fixtures/overture`.
- No new dependency was installed before the human legitimacy gate cleared.
</success_criteria>

<artifacts>
## Artifacts this phase produces (full Phase 1 inventory)

Tables (plan 01-01): `prospects`, `prospect_sources`, `outreach_messages`; column
`scans.prospect_id`.
New deps (this plan): `@duckdb/node-api`, `tldts`, `tsx`, `vitest`.
New types (this plan): `OverturePlaceRow`, `ProspectRow`, `ProspectSourceRow` in types/scanner.ts.
Fixture generator (this plan): `makeOverturePlace` in tests/fixtures/overture.ts.
New functions (plan 01-03): `normalizeDomain`, `DomainValidationError`, `upsertOverturePlace`.
New modules (plan 01-04): `queryOverturePlaces`, `detectCategoryColumn` (lib/overture-client.ts).
New CLI (plan 01-04): `scripts/import-prospects.ts` — flags `--country`, `--region`,
`--category`, `--dry-run`, `--limit`.
</artifacts>

<output>
Create `.planning/phases/01-prospect-data-foundation-import/01-02-SUMMARY.md` when done.
</output>
