---
phase: 04.1-prospect-quality-category-exclusion-gate-split-batch-size
plan: 02
type: execute
wave: 2
depends_on:
  - 04.1-01
files_modified:
  - components/admin/shortlist-table.tsx
  - app/admin/page.tsx
autonomous: false
requirements:
  - 2026-07-21-exclude-food-service-categories-at-triage
  - 2026-07-21-split-triage-gate-unreachable-vs-no-https
  - 2026-07-23-raise-bulk-batch-size-for-daily-cron
user_setup:
  - service: vercel
    why: "Production deploy is manual (Vercel is not git-connected); the harness cannot run npx vercel --prod"
    dashboard_config:
      - task: "Run npx vercel --prod from projects/website-scanner to ship the phase"
        location: "Local terminal in the website-scanner repo"
      - task: "Run the distinct-category SQL to confirm EXCLUDED_CATEGORIES covers real food-service strings"
        location: "Supabase Dashboard -> SQL Editor (production project)"

must_haves:
  truths:
    - The shortlist shows a reachable no-HTTPS row with a CRITICAL pill and sorts it first (D-4.1-04/05)
    - The shortlist shows an unreachable row with a distinct non-priority pill, and it is not counted as eligible and cannot be released (D-4.1-03)
    - A food-service row is visible but not counted in "Eligible now" and cannot be released (D-4.1-01)
    - The "Eligible now" count equals what the Release action will actually release (no drift with selectWorstN)
  artifacts:
    - components/admin/shortlist-table.tsx using isReleasable + CRITICAL/UNREACHABLE pills
    - app/admin/page.tsx eligible/critical counts computed via isReleasable
  key_links:
    - shortlist-table.tsx and app/admin/page.tsx MUST both import isReleasable from lib/triage-eligibility (no re-implemented inline copy)
    - the UI eligible count MUST match selectWorstN — same predicate, same cutoff
---

<objective>
Make the admin Shortlist read honestly: rename the misleading GATED pill to CRITICAL for no-HTTPS priority rows, show unreachable rows as visible-but-not-a-target, exclude food-service and unreachable rows from the eligible count, and ship the whole phase (this UI + Plan 01's gate + the batch-size bump) to production.

Purpose: The UI is what misled Joshua on real data (GATED read as "excluded" when it meant top priority; unreachable rows looked releasable). This plan aligns every count and label with Plan 01's isReleasable predicate so the screen tells the truth, then gates on a human deploy + live check because the harness cannot deploy or reach production.

Output: updated shortlist-table.tsx and admin/page.tsx; a live production shortlist that no longer offers food-service or unreachable rows for release; confirmed public-scanner health after the first bigger daily tick.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-prospect-quality-category-exclusion-gate-split-batch-size/04.1-CONTEXT.md
@.planning/phases/04.1-prospect-quality-category-exclusion-gate-split-batch-size/04.1-01-SUMMARY.md

# UI touched + the predicate/types it consumes (from Plan 01)
@components/admin/shortlist-table.tsx
@app/admin/page.tsx
@lib/triage-eligibility.ts
@lib/triage-candidates.ts
@types/triage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relabel the gate and align the shortlist counts with isReleasable</name>
  <files>components/admin/shortlist-table.tsx, app/admin/page.tsx</files>
  <read_first>
    - components/admin/shortlist-table.tsx (the GATED pill ~line 164-168, the red-priority row styling ~line 159-161, the inline eligible() ~line 117 and eligibleCount ~line 118)
    - app/admin/page.tsx (ShortlistTab: gatedCount ~line 400, eligibleCount ~line 402-404, StatCard "Gated" ~line 411, ReleaseButton/CutoffSlider consumers ~line 420/430)
    - lib/triage-eligibility.ts (isReleasable — import and reuse, do not re-implement)
    - lib/triage-candidates.ts (ShortlistRow now carries category)
    - types/triage.ts (TriageScore.reachable / .https to distinguish unreachable from no-HTTPS)
    - .planning/phases/04.1-prospect-quality-category-exclusion-gate-split-batch-size/04.1-CONTEXT.md (D-4.1-03/04/05)
  </read_first>
  <action>
    In components/admin/shortlist-table.tsx: replace the inline `eligible` helper with isReleasable(row, cutoff) imported from @/lib/triage-eligibility, so the table's eligibleCount and empty-state logic use the exact server predicate (D-4.1-01/03). Split the single conflated priority pill (the one whose exact label the acceptance criteria negative-grep) into two by deriving from the stored booleans on score (TriageScore): for a reachable no-HTTPS row (score.reachable && !score.https) render a pill reading CRITICAL and keep the red-priority left border / bg tint — this is the top-priority signal (D-4.1-04/05). For an unreachable row (!score.reachable) render a distinct muted, non-priority pill reading UNREACHABLE (neutral/grey token, no red-priority border/bg) so it reads as visible-but-not-a-target (D-4.1-03). The prior single conflated pill label that this splits (its exact string is negative-grepped in the acceptance criteria) must no longer render.

    In app/admin/page.tsx ShortlistTab: change eligibleCount to `rows.filter((r) => !r.scan_released_at && isReleasable(r, cutoff)).length` (import isReleasable), so the "Eligible now" StatCard, the CutoffSlider count, and the ReleaseButton all match selectWorstN (D-4.1-01/03 — the count-drift fix). Relabel the "Gated" StatCard to "Critical" and compute its value as reachable no-HTTPS rows (r.triage_score.reachable && !r.triage_score.https), so the stat reflects the priority signal, not the old conflated gate (D-4.1-05). Leave releasedCount, armableCount, and every other behavior unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes (ShortlistRow.category / isReleasable wiring type-checks).
    - `grep -c "CRITICAL" components/admin/shortlist-table.tsx` >= 1 and `grep -c "UNREACHABLE" components/admin/shortlist-table.tsx` >= 1.
    - `grep -c "GATED" components/admin/shortlist-table.tsx` == 0 (old label gone).
    - `grep -c "isReleasable" components/admin/shortlist-table.tsx` >= 1 and `grep -c "isReleasable" app/admin/page.tsx` >= 1 (no inline re-implementation of eligibility).
    - `grep -c "Critical" app/admin/page.tsx` >= 1 (StatCard relabelled).
  </acceptance_criteria>
  <done>The shortlist labels no-HTTPS rows CRITICAL and unreachable rows UNREACHABLE; every eligible count uses isReleasable and matches the server release (D-4.1-01/03/04/05).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm exclusion list against real data, ship to production, verify live</name>
  <what-built>
    The full phase is code-complete and green locally: isReleasable gate (Plan 01), the shortlist UI relabel + count alignment (Task 1), and BULK_BATCH_SIZE = 10. Nothing is live yet — Vercel is not git-connected, so Joshua deploys, and the real category strings + public-scanner health can only be confirmed against production.
  </what-built>
  <action>
    Human-gated: Joshua performs the how-to-verify steps below (SQL confirmation, npx vercel --prod, live shortlist check, post-tick health compare). If step 1 surfaces a real food-service category string missing from EXCLUDED_CATEGORIES, the executor adds it to lib/triage-constants.ts (one-line constant edit) on resume and Joshua redeploys. The harness cannot deploy or read production (.env.local); every live action here is Joshua's.
  </action>
  <how-to-verify>
    1. Confirm the exclusion list against real data (D-4.1-01, "verify real category strings before locking"). In the Supabase Dashboard SQL Editor (production project) run:
       select category, count(*) from prospects where category is not null group by category order by count(*) desc;
       Check every food-service category string in the result appears in EXCLUDED_CATEGORIES (lib/triage-constants.ts). If any real food-service string is missing (e.g. a Dutch/Overture variant not in the seed list), add it to EXCLUDED_CATEGORIES — this is a one-line constant edit the executor applies on resume, then redeploy.
    2. Save a public-scanner health baseline BEFORE the first bigger tick: `npm run scanner-health -- --save pre-04.1` (this reads production via .env.local — Joshua runs it, not the harness).
    3. Ship: from projects/website-scanner run `npx vercel --prod`. Confirm the deploy succeeds.
    4. Live shortlist check (admin -> Shortlist tab) on the deployed URL:
       - A food-service row (e.g. one of the observed restaurants) is visible but is NOT counted in "Eligible now" and the Release action will not release it.
       - An unreachable row (e.g. mollerino.nl) shows the UNREACHABLE pill, is not eligible, cannot be released.
       - A reachable no-HTTPS row shows the CRITICAL pill and sorts first.
       - The "Eligible now" number equals the count the Release button says it will release.
    5. Throughput (D-4.1-06), after the next 07:00 daily drain tick fires: `npm run scanner-health -- --compare pre-04.1` and confirm the public-scanner impact is within tolerance (<= PUBLIC_SCANNER_TOLERANCE_PP, 5pp). This step lands a day later than 1-4; that is expected (the tick is daily).
  </how-to-verify>
  <resume-signal>Type "approved" once steps 1-4 pass (note any category strings you added to EXCLUDED_CATEGORIES). Report the step-5 health-compare result after the next daily tick; if it exceeds 5pp, open a follow-up rather than blocking the phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → admin Shortlist UI / release-prospects route | admin reads counts and triggers release; server enforces releasability |
| operator → production deploy (npx vercel --prod) | manual ship of vetted code |

No new trust boundary is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-03 | Spoofing/UI-trust | eligible count vs actual release | medium | mitigate | UI count and selectWorstN both call isReleasable, so the displayed "Eligible now" cannot promise a release the server will refuse — verified live in the checkpoint. |
| T-04.1-04 | Denial of service | BULK_BATCH_SIZE = 10 vs public scanner | medium | mitigate | 10 is the claim_next_scan_batch server clamp; reserved public-scanner headroom (capacity.ts) is unchanged; checkpoint step 5 measures real impact against the pre-04.1 baseline and holds to <= 5pp. |
| T-04.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run lint` green after the UI change.
- Live: food-service + unreachable rows visible but not eligible/releasable; no-HTTPS row is CRITICAL and first; eligible count matches Release.
- Live: `npm run scanner-health -- --compare pre-04.1` within 5pp after the first bigger tick.
</verification>

<success_criteria>
- The Shortlist tells the truth: CRITICAL = no-HTTPS priority, UNREACHABLE = visible-not-target, counts match the server.
- The phase is deployed to production via npx vercel --prod.
- EXCLUDED_CATEGORIES is confirmed against real production category strings.
- Public-scanner impact stays within tolerance after the daily tick grows to 10.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

Modified symbols:
- components/admin/shortlist-table.tsx: GATED pill removed; CRITICAL pill (reachable no-HTTPS) + UNREACHABLE pill (not reachable); eligible() replaced by isReleasable.
- app/admin/page.tsx ShortlistTab: eligibleCount via isReleasable; "Gated" StatCard relabelled "Critical" (reachable no-HTTPS count).

New user-facing labels: "CRITICAL" (priority pill), "UNREACHABLE" (non-priority pill), "Critical" (stat card).

No new files.
</artifacts_produced>

<output>
Create `.planning/phases/04.1-prospect-quality-category-exclusion-gate-split-batch-size/04.1-02-SUMMARY.md` when done.
</output>
