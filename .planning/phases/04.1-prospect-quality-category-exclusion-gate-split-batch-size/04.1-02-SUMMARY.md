---
phase: 04.1-prospect-quality-category-exclusion-gate-split-batch-size
plan: 02
subsystem: ui
tags: [triage, admin-ui, release-gate, next.js]

# Dependency graph
requires:
  - phase: 04.1-01
    provides: isReleasable predicate, ShortlistRow.category, BULK_BATCH_SIZE=10
provides:
  - Shortlist UI CRITICAL/UNREACHABLE pill split (GATED label removed)
  - Admin eligible/critical counts wired to isReleasable (no divergent UI copy)
  - Production deploy of the full 04.1 phase (predicate + UI + batch size)
affects: [05]

# Tech tracking
tech-stack:
  added: []
  patterns: ["single isReleasable predicate consumed by both the server release path (Plan 01) and the admin UI (Plan 02) — eliminates the count-drift risk that misled Joshua on the first live batch"]

key-files:
  created: []
  modified:
    - components/admin/shortlist-table.tsx
    - app/admin/page.tsx

key-decisions:
  - "D-4.1-05: split the single conflated priority pill into CRITICAL (reachable, no-HTTPS — top priority, keeps red-priority row styling) and UNREACHABLE (not reachable — neutral grey, no priority styling, visible but not a release target)"
  - "app/admin/page.tsx StatCard 'Gated' renamed 'Critical', computed as reachable+no-HTTPS count (not the raw `gated` boolean, which among reachable rows means the same thing but would also count an excluded-category reachable no-HTTPS row as 'critical' rather than as a priority signal only)"
  - "eligibleCount in both shortlist-table.tsx and app/admin/page.tsx now calls isReleasable(row, cutoff) directly — no inline gated/cutoff re-implementation, so the UI count can never drift from selectWorstN"

patterns-established:
  - "Admin UI counts/labels derive from the same predicate module as the server release path, never a parallel inline check"

requirements-completed:
  - 2026-07-21-exclude-food-service-categories-at-triage
  - 2026-07-21-split-triage-gate-unreachable-vs-no-https
  - 2026-07-23-raise-bulk-batch-size-for-daily-cron

coverage:
  - id: D1
    description: "Shortlist relabels the priority pill to CRITICAL (reachable, no-HTTPS) and UNREACHABLE (not reachable); every eligible/critical count in the admin UI derives from isReleasable or the same triage_score booleans the server uses — no re-implemented eligibility logic"
    requirement: 2026-07-21-split-triage-gate-unreachable-vs-no-https
    verification:
      - kind: unit
        ref: "npx tsc --noEmit; npm run lint; npx vitest run (214/214 passing); acceptance-criteria greps (CRITICAL >=1, UNREACHABLE >=1, GATED ==0, isReleasable in both files, Critical StatCard label)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Production deploy shipped and live-verified: a food-service row is visible but not counted in Eligible-now and cannot be released; an unreachable row shows the UNREACHABLE pill and is not eligible/releasable; a reachable no-HTTPS row shows CRITICAL and sorts first; Eligible-now count matches what Release will actually release"
    requirement: 2026-07-21-exclude-food-service-categories-at-triage
    verification:
      - kind: manual_procedural
        ref: "Joshua production verification 2026-07-24 (npx vercel --prod succeeded; SQL category cross-check against EXCLUDED_CATEGORIES found no missing strings; 4 live admin Shortlist checks all passed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "BULK_BATCH_SIZE=10 live in production after redeploy; post-tick public-scanner health impact stays within tolerance (<=5pp) after the next daily drain claims up to 10"
    requirement: 2026-07-23-raise-bulk-batch-size-for-daily-cron
    verification: []
    human_judgment: true
    rationale: "The scanner-health --compare pre-04.1 result depends on the next 07:00 daily drain tick, which has not fired yet as of plan close. Reported separately per the checkpoint resume-signal; if it exceeds 5pp a follow-up gets opened rather than blocking this phase."

# Metrics
duration: 15min active + overnight human-verification checkpoint (2026-07-23 to 2026-07-24)
completed: 2026-07-24
status: complete
---

# Phase 04.1 Plan 02: Shortlist UI Truth-Telling + Production Ship Summary

**Split the misleading GATED pill into CRITICAL (no-HTTPS priority) and UNREACHABLE (visible, not a target); wired every admin eligible/critical count to the same isReleasable predicate the server uses; shipped the whole 04.1 phase to production and live-verified it.**

## Performance

- **Duration:** ~15 min active work (Task 1) + an overnight checkpoint pause for Joshua's production verification (Task 2)
- **Started:** 2026-07-23T22:15:23Z
- **Completed:** 2026-07-24
- **Tasks:** 2 (1 auto, 1 checkpoint:human-verify)
- **Files modified:** 2

## Accomplishments
- `components/admin/shortlist-table.tsx`: the single conflated priority pill is split into `CRITICAL` (reachable + no-HTTPS, keeps the red-priority row border/tint) and `UNREACHABLE` (not reachable, neutral grey, no priority styling) — the old `GATED` label no longer renders anywhere
- `eligibleCount` in both `shortlist-table.tsx` and `app/admin/page.tsx` now calls `isReleasable(row, cutoff)` from `lib/triage-eligibility.ts` directly, replacing the inline `gated || score <= cutoff` check — the "Eligible now" stat, the CutoffSlider, and the ReleaseButton can no longer drift from `selectWorstN`
- `app/admin/page.tsx` `ShortlistTab`: the "Gated" StatCard is relabelled "Critical" and computed from `reachable && !https`, reflecting the priority signal rather than the old conflated gate boolean
- Production deploy shipped (`npx vercel --prod`, run by Joshua) with the full 04.1 phase live: the `isReleasable` predicate + `EXCLUDED_CATEGORIES` (Plan 01), this UI relabel (Plan 02 Task 1), and `BULK_BATCH_SIZE=10`
- Live verification (Joshua, 2026-07-24): SQL category cross-check against `EXCLUDED_CATEGORIES` found no missing food-service strings; food-service rows visible but not eligible/releasable; `mollerino.nl` shows `UNREACHABLE` and is not eligible/releasable; a reachable no-HTTPS row shows `CRITICAL` and sorts first; "Eligible now" matches what Release actually releases

## Task Commits

Each task was committed atomically:

1. **Task 1: Relabel the gate and align the shortlist counts with isReleasable** - `58484df` (feat)
2. **Task 2: Confirm exclusion list against real data, ship to production, verify live** - human-verified 2026-07-24, no code changes (no missing category strings found, no commit needed)

**Plan metadata:** committed separately after this SUMMARY (docs: complete plan)

## Files Created/Modified
- `components/admin/shortlist-table.tsx` - GATED pill split into CRITICAL/UNREACHABLE; eligibleCount now calls isReleasable
- `app/admin/page.tsx` - eligibleCount calls isReleasable; "Gated" StatCard renamed "Critical", computed from reachable+no-HTTPS

## Decisions Made
- CRITICAL styling (red-priority border/bg) applies only to reachable no-HTTPS rows, not to unreachable rows — unreachable is "visible but not a target," not a priority signal (D-4.1-03 vs D-4.1-04/05)
- The "Critical" StatCard counts `reachable && !https` directly rather than the raw `gated` boolean, so an excluded-category reachable no-HTTPS row still counts as a priority signal for display even though it is barred from release by `isReleasable`
- No category strings were missing from `EXCLUDED_CATEGORIES` when checked against real production data (2026-07-24) — the Plan 01 seed list (18 Overture eat-and-drink strings) stands as committed, no follow-up edit needed

## Deviations from Plan

**1. [Documentation-only, no code change] Reworded two source comments that referenced the old "GATED" label**
- **Found during:** Task 1 verification (acceptance-criteria grep)
- **Issue:** The acceptance criterion `grep -c "GATED" components/admin/shortlist-table.tsx == 0` failed at 2 — not because the old pill still rendered, but because two prose comments (a status-pill-styling note and the pill-split explanation) referenced the old label name for context
- **Fix:** Reworded both comments to reference "CRITICAL/UNREACHABLE priority pills" instead of "GATED pill" — no rendering logic touched
- **Files modified:** components/admin/shortlist-table.tsx
- **Verification:** `grep -c "GATED"` now returns 0; all other acceptance-criteria greps unchanged and passing
- **Committed in:** 58484df (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (documentation wording only, Rule 1 scope — kept the acceptance criteria's literal grep and the intended semantics aligned)
**Impact on plan:** No functional change. No scope creep.

## Issues Encountered

None. `npx tsc --noEmit`, `npm run lint`, and the full `npx vitest run` (214/214) all passed on first run after Task 1.

## User Setup Required

None remaining. Task 2's `user_setup` (SQL confirmation + `npx vercel --prod`) was completed by Joshua during the checkpoint — see Accomplishments above.

## Next Phase Readiness

- The Shortlist tells the truth in production: CRITICAL = no-HTTPS priority, UNREACHABLE = visible-not-target, every count matches the server's `isReleasable`.
- `BULK_BATCH_SIZE=10` is live; the post-tick public-scanner health compare (`npm run scanner-health -- --compare pre-04.1`) is an **open follow-up** — it depends on the next 07:00 daily drain and will be reported separately. If it exceeds the 5pp tolerance, open a follow-up phase/todo rather than reopening this plan.
- No blockers for Phase 05. `isReleasable`, `ShortlistRow.category`, and the CRITICAL/UNREACHABLE UI convention are all available for downstream phases to reuse.

## Self-Check: PASSED

`components/admin/shortlist-table.tsx` and `app/admin/page.tsx` modifications found on disk. Commit `58484df` found in git log. This SUMMARY.md written to disk at the expected path.

---
*Phase: 04.1-prospect-quality-category-exclusion-gate-split-batch-size*
*Completed: 2026-07-24*
