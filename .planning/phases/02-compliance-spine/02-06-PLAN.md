---
phase: 02-compliance-spine
plan: 06
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - scripts/suppression-override.ts
  - scripts/suppression-override.test.ts
  - scripts/legal-basis.ts
  - scripts/legal-basis.test.ts
autonomous: true
requirements: [CMP-06, CMP-08, CMP-16]
must_haves:
  truths:
    - "scripts/suppression-override.ts is the only path that can re-enable contact for a suppressed address: it lifts the active row (sets lifted_at + lifted_by_reason), never deletes, and requires explicit --email + --reason (CMP-06, D-08, D-09)."
    - "After a lift, matching ignores the lifted row; a later re-suppression adds a NEW row (D-09)."
    - "scripts/legal-basis.ts resolves a domain-or-email to its prospect country -> legal_regimes row -> current LIA version, and reports current suppression status, in one output (CMP-08, CMP-16, D-10)."
  artifacts:
    - "scripts/suppression-override.ts + scripts/suppression-override.test.ts"
    - "scripts/legal-basis.ts + scripts/legal-basis.test.ts"
  key_links:
    - "suppression-override -> liftSuppression (Plan 01)."
    - "legal-basis -> prospect country -> legal_regimes.current_lia_version -> lia_versions (Plan 02) + isSuppressed (Plan 01)."
  prohibitions:
    - "suppression-override must never DELETE a suppression row — lift only (D-09, aligns with CMP-15 indefinite retention)."
    - "No wildcard/bulk mode in suppression-override — a single explicit --email + --reason per run (threat: accidental mass-override)."
    - "legal-basis must resolve the regime from the legal_regimes config table, never from a hardcoded country branch (CMP-16)."
    - "Neither script mutates prospects.lifecycle_state (D-07)."
---

<objective>
Ship the two operator CLI surfaces this phase owns (Phase 3 later promotes the same logic to UI): the logged override that lifts a suppression (CMP-06, the only escape hatch), and the legal-basis lookup that answers "which LIA version and country regime applies, and is this address suppressed?" (CMP-08, CMP-16).

Purpose: Satisfy CMP-06's "explicit, logged manual override" and CMP-08/16's lookup without building throwaway UI ahead of Phase 3 (D-08, D-09, D-10).
Output: `scripts/suppression-override.ts`, `scripts/legal-basis.ts`, and their DI-seam tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compliance-spine/02-CONTEXT.md
@.planning/phases/02-compliance-spine/02-RESEARCH.md
@.planning/phases/02-compliance-spine/02-PATTERNS.md
@lib/suppression.ts
</context>

## Artifacts this phase produces

Full phase inventory (this plan owns the ones marked ★):
- `supabase/migrations/014_create_suppressions.sql`, `lib/suppression.ts` (Plan 01)
- `supabase/migrations/015_create_legal_basis.sql`, `docs/legal/lia/LIA-v1.md` (Plan 02)
- `lib/unsubscribe-token.ts` (Plan 03), `app/api/unsubscribe/[token]/route.ts` (Plan 04)
- extended `app/api/webhooks/resend/route.ts`, `scripts/backfill-suppressions.ts` (Plan 05)
- ★ `scripts/suppression-override.ts` — D-08/D-09 logged lift
- ★ `scripts/legal-basis.ts` — D-10 country → regime → LIA version + suppression status

<tasks>

<task type="auto">
  <name>Task 1: scripts/suppression-override.ts (logged lift, no delete, no bulk) + DI-seam test</name>
  <files>scripts/suppression-override.ts, scripts/suppression-override.test.ts</files>
  <read_first>
    - scripts/import-prospects.ts (CLI shape: header usage block, `parseArgs` strict, fail-closed required-flag validation with `ImportArgsError`, DI-seam Deps + runCli — lines 1-110)
    - scripts/import-prospects.test.ts (arg-validation + DI-stub test pattern — lines 1-70)
    - lib/suppression.ts (liftSuppression(sb, { email, reason }) — Plan 01)
    - .planning/phases/02-compliance-spine/02-CONTEXT.md (D-08 CLI override, D-09 lifts-never-deletes)
    - .planning/phases/02-compliance-spine/02-RESEARCH.md (Security Domain "Accidental mass-override" threat — explicit required args, no wildcard/bulk)
  </read_first>
  <action>
    Create `scripts/suppression-override.ts` mirroring import-prospects.ts. Usage block: `npx tsx scripts/suppression-override.ts --email=<address> --reason="<why>"`. `parseArgs` (strict) with `email` and `reason` string options; a fail-closed `parseOverrideArgs(argv)` that throws an `OverrideArgsError` listing any missing required flag (both `--email` and `--reason` are required). The script operates on exactly one address per run — accept no bulk/match-all flag and no glob input. `OverrideDeps` DI interface (`createServerClient`, `liftSuppression`). `runOverride(deps, args)`: build the service client, call `liftSuppression(sb, { email, reason })`; print exactly what it did — the email, the reason, and whether an active row was found and lifted (or that none existed). The row is lifted (its lifted_at/lifted_by_reason set), never removed; never touch prospects. `runCli()` entry parses argv and awaits runOverride, printing the summary. Then create `scripts/suppression-override.test.ts` mirroring import-prospects.test.ts: assert `parseOverrideArgs` throws `OverrideArgsError` when `--email` or `--reason` is missing; assert `runOverride` calls the injected `liftSuppression` exactly once with the given email+reason and prints the outcome; assert the code lifts rather than removes rows.
  </action>
  <verify>
    <automated>npx vitest run scripts/suppression-override.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test command passes: missing `--email` or `--reason` throws; a valid run invokes liftSuppression once.
    - Source assertion (comment-stripped): `grep -vE '^\s*//' scripts/suppression-override.ts | grep -q "\.delete("` finds nothing (lift-only, never removes a row).
    - Source assertion: the script imports `liftSuppression` and issues no update/write to the prospects table.
  </acceptance_criteria>
  <done>The override CLI lifts an active suppression with an explicit logged reason, requires both --email and --reason, never deletes or bulk-operates, and is proven by an arg-validation + DI-stub test.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/legal-basis.ts (country → regime → LIA version + suppression status) + DI-seam test</name>
  <files>scripts/legal-basis.ts, scripts/legal-basis.test.ts</files>
  <read_first>
    - scripts/import-prospects.ts (CLI shape + DI-seam + fail-closed args)
    - scripts/import-prospects.test.ts (DI-stub test pattern)
    - lib/suppression.ts (isSuppressed — for the suppression-status line), lib/domain-normalize.ts (normalizeDomain to resolve a domain-or-email input)
    - supabase/migrations/010_create_prospects.sql (prospects.country, prospects.domain, prospects.contact_email — how to look up the prospect)
    - supabase/migrations/015_create_legal_basis.sql (legal_regimes country_code/spam_law_regime/current_lia_version, lia_versions — Plan 02)
    - .planning/phases/02-compliance-spine/02-CONTEXT.md (D-10 one-output lookup, D-12 per-country regime points at LIA version)
  </read_first>
  <action>
    Create `scripts/legal-basis.ts` mirroring import-prospects.ts. Usage: `npx tsx scripts/legal-basis.ts --email=<address>` or `--domain=<domain>` (at least one required; fail closed with `LegalBasisArgsError`). `LegalBasisDeps` DI interface (`createServerClient`, `isSuppressed`). `runLegalBasis(deps, args)`: normalise the input to a registrable domain via `normalizeDomain`; look up the prospect (`.eq("domain", …)` or by email) to get its `country`; look up the `legal_regimes` row for that country to get `spam_law_regime` + `current_lia_version`; look up the matching `lia_versions` row (version, effective_from, content_hash); call `isSuppressed(sb, email)` (when an email was given, else check the domain via a representative address) for the suppression-status line. Print ONE consolidated output block: input, resolved country, spam_law_regime, notes_url, current LIA version + effective_from + content_hash, and suppression status. Resolve the regime ONLY from the config table — no hardcoded country branch. Then create `scripts/legal-basis.test.ts` mirroring import-prospects.test.ts: stub the Supabase lookups to return an NL prospect + the seeded NL regime + LIA v1, stub isSuppressed, and assert the output reports country NL, `opt-out-narrow-exemption`, LIA version 1, and the suppression status; assert missing-arg throws `LegalBasisArgsError`.
  </action>
  <verify>
    <automated>npx vitest run scripts/legal-basis.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test command passes: an NL prospect resolves to the narrow-exemption regime + LIA v1 + suppression status in one output; missing arg throws.
    - Source assertion: `grep -qiE "=== ['\"]NL['\"]|country == ['\"]NL" scripts/legal-basis.ts` returns nothing (no hardcoded country branch — CMP-16).
    - Source assertion: the script reads `legal_regimes` and `lia_versions` and imports `isSuppressed`.
  </acceptance_criteria>
  <done>The legal-basis CLI resolves a domain-or-email to its country, regime, and current LIA version from config, reports suppression status in one output, and never hardcodes NL logic — proven by a DI-stub test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → suppression lift | Only someone with deploy-shell access can run the override; same posture as scripts/import-prospects.ts (no new access-control primitive). |
| config table → legal answer | The legal basis reported must come from legal_regimes, not from code, so expansion is a data change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-20 | Tampering / Repudiation | accidental mass-override re-enables many suppressed addresses | high | mitigate | Explicit required --email + --reason, no wildcard/bulk mode, prints what it did (Task 1) |
| T-02-21 | Repudiation | override deletes the suppression, erasing the audit trail | high | mitigate | liftSuppression sets lifted_at/lifted_by_reason; no delete path; grep gate in acceptance (Task 1, D-09/CMP-15) |
| T-02-22 | Tampering | hardcoded NL legal logic diverges from config | medium | mitigate | legal-basis resolves regime from legal_regimes only; grep gate rejects a hardcoded country branch (Task 2, CMP-16) |
| T-02-23 | Information Disclosure | anon access to legal/suppression data via the script | low | accept | Scripts use the service-role client from the deploy shell only; no anon path exists |
| T-02-SC | Tampering | new package supply-chain | low | accept | Zero new packages — reuses node:util, supabase-js, tldts |
</threat_model>

<verification>
- `npx vitest run scripts/suppression-override.test.ts` and `npx vitest run scripts/legal-basis.test.ts` green.
- Source review: override is lift-only with no bulk mode; legal-basis has no hardcoded NL branch; neither mutates prospects.
</verification>

<success_criteria>
Re-adding a suppressed record is impossible without the explicit logged override (which lifts, never deletes), and Joshua can look up which LIA version and country regime applies to any prospect plus its suppression status in one command — CMP-06, CMP-08, CMP-16 satisfied at the operator surface.
</success_criteria>

<output>
Create `.planning/phases/02-compliance-spine/02-06-SUMMARY.md` when done.
</output>
