---
phase: 02-compliance-spine
plan: 07
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: []
autonomous: false
requirements: [CMP-01, CMP-08, CMP-16]
user_setup:
  - service: supabase
    why: "Migrations 014 and 015 must be applied to the live Supabase project before the phase can pass verification — build/type checks pass without it, creating a false-positive state."
    dashboard_config:
      - task: "Apply migrations 014_create_suppressions.sql and 015_create_legal_basis.sql to production"
        location: "Supabase Dashboard → SQL Editor (project convention for 010-013, STATE.md Phase 01-01), or `supabase db push` if SUPABASE_ACCESS_TOKEN + linked project ref are configured"
must_haves:
  truths:
    - "The suppressions, lia_versions, and legal_regimes tables exist in the LIVE Supabase project (not only local), with the partial unique index, immutability trigger, and NL seed row present."
    - "The push happens after both migration files are written and before phase verification — a green build alone is a false positive."
  artifacts:
    - "No new files — this plan applies migrations 014 and 015 to production."
  key_links:
    - "Live suppressions table backs CMP-01 (source of truth); live legal_regimes + lia_versions back CMP-08/CMP-16."
  prohibitions:
    - "Do not run against a local/dev database and report it as the production push — this must hit the live project."
    - "Do not skip or auto-approve this gate: verification cannot pass without the live schema."
---

<objective>
Apply migrations 014 (suppressions) and 015 (legal basis) to the LIVE Supabase project. This is the mandatory [BLOCKING] schema-push gate: the app's build and type checks pass from config/generated types without the live DB, so without this step the phase would verify green while the tables do not actually exist in production.

Purpose: Make the compliance spine real in production — CMP-01's source of truth and CMP-08/16's legal config must exist where the app runs, not only in local tests.
Output: Both migrations applied to prod; the three tables + partial unique index + immutability trigger + NL seed confirmed present.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-compliance-spine/02-RESEARCH.md
@supabase/migrations/014_create_suppressions.sql
@supabase/migrations/015_create_legal_basis.sql
</context>

## Artifacts this phase produces

This plan creates no files. It applies to production the migrations authored in Plans 01 and 02:
- `supabase/migrations/014_create_suppressions.sql` (Plan 01)
- `supabase/migrations/015_create_legal_basis.sql` (Plan 02)

Full phase symbol inventory: `suppressions`, `lia_versions`, `legal_regimes` tables; `lib/suppression.ts` (`isSuppressed`, `writeSuppression`, `liftSuppression`); `lib/unsubscribe-token.ts` (`signUnsubscribeToken`, `verifyUnsubscribeToken`); `app/api/unsubscribe/[token]/route.ts`; extended `app/api/webhooks/resend/route.ts`; `scripts/backfill-suppressions.ts`, `scripts/suppression-override.ts`, `scripts/legal-basis.ts`; `docs/legal/lia/LIA-v1.md`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Apply migrations 014 + 015 to the live Supabase project</name>
  <files>supabase/migrations/014_create_suppressions.sql, supabase/migrations/015_create_legal_basis.sql</files>
  <read_first>
    - .planning/STATE.md (Phase 01-01 decision: migrations 010-013 applied via Supabase Dashboard SQL Editor, not `supabase db push` — all DDL is IF NOT EXISTS/idempotent so a CLI push stays safe)
    - supabase/migrations/014_create_suppressions.sql (Plan 01)
    - supabase/migrations/015_create_legal_basis.sql (Plan 02)
  </read_first>
  <action>
    [BLOCKING] Apply migrations 014 and 015 to the LIVE Supabase project, then confirm the schema landed. Preferred path if configured: `supabase db push` with `SUPABASE_ACCESS_TOKEN` set and the project ref linked (non-interactive once linked). Fallback per project convention (STATE.md Phase 01-01): paste `supabase/migrations/014_create_suppressions.sql` then `015_create_legal_basis.sql` into the Supabase Dashboard → SQL Editor and run them in order. Both files are idempotent (`if not exists`, `on conflict do nothing`), so a re-apply is safe. This is a human gate because the push targets production and, per convention, is applied via the dashboard — do not auto-approve.
  </action>
  <verify>
    <human-check>Confirm in the live project that `suppressions`, `lia_versions`, and `legal_regimes` exist; that `suppressions_email_active_idx` is present; that the NL `legal_regimes` row points at LIA version 1 and `lia_versions` has a version-1 row; and that `update lia_versions set content_hash='x' where version=1;` raises an error (immutability trigger fired).</human-check>
  </verify>
  <done>All three tables, the partial unique index, the NL seed row, and the immutability trigger are confirmed present in the LIVE Supabase project.</done>
  <what-built>
    Migrations 014 (suppressions table + `suppressions_email_active_idx` partial unique index + RLS) and 015 (`lia_versions` + `legal_regimes` + `prevent_lia_versions_mutation()` trigger + NL seed row) are written and pass local integration tests (`supabase db reset`). They now need to be applied to the LIVE Supabase project so the app can use them in production. Both files are fully idempotent (`if not exists`, `on conflict do nothing`), so a re-apply is safe.
  </what-built>
  <how-to-verify>
    1. Apply both migrations to production, using whichever path is configured:
       - Preferred if configured: `supabase db push` (ensure `SUPABASE_ACCESS_TOKEN` is set and the project ref is linked; this is non-interactive when the ref is already linked).
       - Fallback (project convention, STATE.md Phase 01-01): paste the contents of `supabase/migrations/014_create_suppressions.sql` then `015_create_legal_basis.sql` into the Supabase Dashboard → SQL Editor and run them, in order.
    2. Confirm in the live project (SQL Editor or `supabase db` inspection) that all three tables exist: `suppressions`, `lia_versions`, `legal_regimes`.
    3. Confirm the `suppressions_email_active_idx` partial unique index exists on `suppressions`.
    4. Confirm the NL row exists: `select country_code, spam_law_regime, current_lia_version from legal_regimes where country_code = 'NL';` returns one row pointing at LIA version 1, and `select version, content_hash from lia_versions where version = 1;` returns one row.
    5. Confirm the immutability trigger fires: an `update lia_versions set content_hash = 'x' where version = 1;` returns an error.
  </how-to-verify>
  <resume-signal>Type "approved" once all three tables, the partial unique index, the NL seed row, and the immutability trigger are confirmed present in the live Supabase project. Describe any discrepancy instead if a step failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local schema → production schema | Local integration tests are not proof of production state; the live push is the only thing that makes the spine real where the app runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-24 | Repudiation | phase verifies green while suppressions does not exist in prod (false positive), so a real send later has no suppression table | high | mitigate | Mandatory blocking human-verify gate confirming the three live tables + index + trigger before phase verification (Task 1) |
| T-02-25 | Tampering | wrong/dev database targeted by the push | medium | mitigate | Verification step reads back the live project's tables and NL seed row explicitly (Task 1) |
| T-02-SC | Tampering | new package supply-chain | low | accept | Zero new packages this phase |
</threat_model>

<verification>
- Human confirms `suppressions`, `lia_versions`, `legal_regimes` exist in the live project, the partial unique index and immutability trigger are present, and the NL seed row resolves to LIA version 1.
</verification>

<success_criteria>
The compliance-spine schema is live in production, so CMP-01's suppression source of truth and CMP-08/16's legal config exist where the app actually runs — the phase can pass verification against real infrastructure, not a false-positive build.
</success_criteria>

<output>
Create `.planning/phases/02-compliance-spine/02-07-SUMMARY.md` when done.
</output>
