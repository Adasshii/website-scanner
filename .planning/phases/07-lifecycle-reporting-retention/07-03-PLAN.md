---
phase: 07-lifecycle-reporting-retention
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - lib/lifecycle.ts
  - lib/reporting-aggregates.ts
  - lib/reporting-aggregates.integration.test.ts
  - lib/reporting-format.test.ts
  - components/admin/reporting-tab.tsx
  - app/admin/reporting-gate.test.tsx
autonomous: true
requirements: [TRK-03, TRK-05]

must_haves:
  truths:
    - "Reply rate with zero contacted prospects renders the awaiting state, never `NaN`, `Infinity`, or a division error."
    - "Two distinct empty copies (zero prospects ever vs. prospects exist but zero 30-day activity); see the UI-SPEC Copywriting Contract."
    - "Same panel-level `Loading...` treatment as the funnel cards; no per-row skeleton."
    - "Same red error banner and copy pattern as `fetchShortlist`/`fetchOutreach`, noun swapped to `reporting data`."
    - "30 rows, newest day first; columns Date / Imported / Triaged / Scanned / Contacted / Reply rate / Booked."
    - "Table wrapped in the existing `overflow-x-auto` convention — horizontal scroll on narrow admin viewports, same as every other admin table."
    - "Always exactly 30 calendar-day rows: a fixed window (D-7-12), not a filtered activity list, so no zero/one/many layout or copy branching exists."
    - "Cells hold only dates, integers, and one fixed placeholder. The one unbounded string is the interpolated `{detail}` in the error copy, which renders inside the existing wrapping error banner (no `truncate` / `whitespace-nowrap`), so a long detail wraps rather than overflowing its container."
    - "Every per-day bucket is a UTC calendar day (`toISOString().slice(0, 10)`), so an event's bucket does not shift with the server's session timezone or with a CET/CEST transition."
    - statement: "The displayed reply rate is rounded to a single defined precision and never renders a raw float artifact."
      verification: backstop
    - statement: "While the global sent-gate is open, every row renders Imported/Triaged/Scanned/Contacted as real numbers while the Reply rate and Booked cells render `— Not yet sending`; when the gate flips, all cells render real numbers with no other UI change."
      verification: backstop
  artifacts:
    - lib/reporting-aggregates.ts
    - components/admin/reporting-tab.tsx
    - lib/reporting-format.test.ts
    - app/admin/reporting-gate.test.tsx
  key_links:
    - "`REPLY_SIGNAL_AVAILABLE` (`lib/lifecycle.ts`) -> `ReportingDay.replyRate`. `deriveLifecycleState()` can never return `replied` in Phase 7, so a reply rate computed from it would read a truthful-looking `0%` the moment Phase 8 sends without also shipping the reply marker. The constant is the single flip point Phase 8 must find. This is what makes D-7-R1 structural rather than a note: reply-rate and booked-call figures read empty until Phase 8 by design, because Phase 7 builds the state machine, the transitions that already have events, and the counters, while Phase 8 calls the hooks."
    - "`utcDay()` -> every one of the five source timestamps (`prospects.created_at`, `triage_checked_at`, `scans.created_at`, `outreach_messages.sent_at`, `prospects.booked_at`). One bucketing function for all five, or the columns of a single row describe different days."
  prohibitions:
    - "The per-day Booked figure must never present a domain-inferred booking as a certain one. `booked_match_method` exists so a guess stays distinguishable from a fact (D-7-06/07); a Booked number that silently sums `email` and `domain` matches into one undifferentiated figure is Joshua reporting a guess back to himself as pipeline truth, and nothing in the schema or the UI contract forbids it."
---

<objective>
Finish the Reporting tab: the 30-day per-day table that answers TRK-05 ("how many prospects were
imported, triaged, scanned and contacted per run") and the reply-rate column that answers TRK-03
honestly rather than with a plausible zero.

Purpose: `07-RESEARCH.md` Pitfall 2 establishes that TRK-03 is not merely a gated zero. There is
no `replied_at` column, no reply-detection webhook and no event log anywhere in this codebase; the
`replied` state is a placeholder Phase 8 owns. This plan therefore builds the counters TRK-05 needs
from timestamps that genuinely exist, and makes the reply-rate cell structurally incapable of
rendering a number until a reply marker exists.

Output: `ReportingDay[]` in the payload, the rendered table, both empty states, and the E2 backstop
render test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-lifecycle-reporting-retention/07-CONTEXT.md
@.planning/phases/07-lifecycle-reporting-retention/07-UI-SPEC.md
@.planning/phases/07-lifecycle-reporting-retention/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-day UTC aggregates and the reply-rate capability gate</name>
  <read_first>
    - `lib/reporting-aggregates.ts` (written in plan 07-02) — `ReportingPayload`, `FunnelCounts`,
      `getReportingData()`, and the injected-client convention being extended
    - `lib/lifecycle.ts` (written in plan 07-02) — where `REPLY_SIGNAL_AVAILABLE` is added, and the
      comment block explaining that `replied` has no rung
    - `lib/reporting-aggregates.integration.test.ts` (written in plan 07-02) — the existing
      fixture prefix and `afterEach` cleanup this task extends rather than duplicates
    - `07-RESEARCH.md` § Common Pitfalls #6 (UTC day bucketing, no timezone library) and #2 (the
      reply-rate data-source gap)
    - `07-CONTEXT.md` D-7-10 (a run is a calendar day, derived from timestamps, no `runs` table),
      D-7-12 (30-day window), D-7-13 (honest absence over a plausible zero)
    - `supabase/migrations/013_add_prospect_id_to_scans.sql` — `scans.prospect_id`, the filter that
      keeps public-scanner scans out of the `scanned` count
    - `supabase/migrations/012_create_outreach_messages.sql` — `sent_at`, the `contacted` source
  </read_first>
  <behavior>
    `lib/reporting-aggregates.integration.test.ts` (extended) asserts:
    - `days` has exactly 30 entries, newest first, and every `date` is a distinct `YYYY-MM-DD`
      string with no gaps.
    - A prospect seeded with `created_at` at `23:59:59Z` and another at `00:00:01Z` the next day
      land in two different buckets, and neither shifts when the process `TZ` is set to
      `Europe/Amsterdam` (assert with `TZ` set inside the test).
    - Per-day `imported` / `triaged` / `scanned` / `contacted` counts match hand-seeded fixture
      rows, including a day with zero of everything rendering `0` rather than being omitted.
    - A `scans` row with `prospect_id` NULL (a public-scanner scan) is NOT counted into `scanned`.
    - An event older than 30 days is excluded from every bucket but still counted in the funnel.
    - `replyRate` is `null` on every one of the 30 days while `REPLY_SIGNAL_AVAILABLE` is false,
      including on a day whose `contacted` count is greater than zero.
    - `booked` is `null` on every day while `sentGateOpen` is false, and an integer once it is true.
    - `bookedByDomain` never exceeds `booked` on any day.

    `lib/reporting-format.test.ts` (unit) asserts:
    - `formatReplyRate(0.3333333)` returns a string with exactly one defined precision and no
      trailing float artifact.
    - `formatReplyRate(0)` returns that same precision applied to zero, not an empty string.
    - `formatReplyRate(1)` returns the whole-number form.
    - `formatReplyRate(null)` returns the awaiting literal, never `NaN%`, `Infinity%` or `null%`.
  </behavior>
  <action>
    **`lib/lifecycle.ts`** — add one export:
    `export const REPLY_SIGNAL_AVAILABLE = false;` with a comment stating that no reply marker
    exists anywhere in this codebase (no column, no webhook, no event log), that
    `deriveLifecycleState()` therefore can never return `replied`, and that Phase 8 must flip this
    constant in the same change that adds the marker and the ladder rung. Name it as the single
    flip point so Phase 8 does not have to find the gate by reading the reporting module. Change
    nothing else in this file.

    **`lib/reporting-aggregates.ts`** — extend.

    Export `utcDay(ts: string): string` returning `new Date(ts).toISOString().slice(0, 10)`, with
    a comment recording that UTC is the deliberate choice (Pitfall 6): every timestamp in this
    database is `timestamptz` stored as UTC, no timezone library is installed, and grouping
    without naming a zone would silently follow the query engine's session timezone. All five
    source timestamps bucket through this one function.

    Export `interface ReportingDay` with `date: string`, `imported: number`, `triaged: number`,
    `scanned: number`, `contacted: number`, `replyRate: number | null`, `booked: number | null`,
    `bookedByDomain: number | null`.

    Extend `ReportingPayload` with `days: ReportingDay[]`.

    Extend `getReportingData()`:
    - Build the 30-day window as an array of `utcDay()` strings from today backwards, newest first.
      This is a fixed window (D-7-12), never a filtered list of days that had activity, so all 30
      rows exist even when every count is zero.
    - `imported` from `prospects.created_at`; `triaged` from `prospects.triage_checked_at`; both
      come from the prospect rows already fetched for the funnel, so add no query.
    - `scanned` from a new SELECT of `created_at` from `scans` filtered
      `.not("prospect_id", "is", null)`. That filter is D-7-16's scope line applied to reporting:
      the public scanner's own scans are not prospect activity and must not inflate this count.
    - `contacted` from `outreach_messages.sent_at`, non-null only, from the outreach rows already
      fetched for the sent-gate.
    - `booked` from `prospects.booked_at`, and `bookedByDomain` from the subset whose
      `booked_match_method` is `domain`. Both are `null` while `sentGateOpen` is false. Add
      `booked_match_method` to the prospects SELECT.
    - `replyRate` is `null` whenever `REPLY_SIGNAL_AVAILABLE` is false, whenever `sentGateOpen` is
      false, and whenever that day's `contacted` is zero. Only when all three clear does it compute
      `replied / contacted`. The zero-contacted clause is what keeps a division from ever producing
      `NaN` or `Infinity`; the capability clause is what stops a truthful-looking `0%` the moment
      Phase 8 sends without shipping the reply marker. Both guards stay, they answer different
      failure modes.

    Restrict every day-bucket query to the 30-day window with a `.gte()` on the relevant timestamp
    so the queries do not widen as the table grows. The funnel queries stay unbounded, because the
    funnel describes where every prospect stands now.

    **`lib/reporting-format.ts`** (new, small) — export
    `formatReplyRate(rate: number | null): string`. `null` returns the awaiting literal from the
    UI-SPEC Copywriting Contract. A number returns a whole-percent string via `Math.round`, one
    defined precision, no decimals. Keeping the formatter in a plain module rather than inline in
    the component is what makes the precision backstop testable without a DOM.
  </action>
  <verify>
    <automated>npx vitest run lib/reporting-format.test.ts && npx vitest run lib/reporting-aggregates.integration.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/reporting-aggregates.integration.test.ts` passes, including the two
      day-boundary fixtures asserted under `TZ=Europe/Amsterdam`.
    - `npx vitest run lib/reporting-format.test.ts` passes with at least 4 tests.
    - `getReportingData()` returns `days.length === 30` for a database with zero rows.
    - `grep -c 'REPLY_SIGNAL_AVAILABLE' lib/reporting-aggregates.ts` returns at least 1 — the
      reply-rate branch reads the constant rather than only the sent-gate.
    - `grep -v '^\s*//' lib/reporting-aggregates.ts | grep -c 'prospect_id' ` returns at least 1,
      confirming the `scanned` count carries the prospect-ownership filter in code, not in a
      comment.
    - `formatReplyRate` is the only place a rate becomes a string; the component imports it rather
      than formatting inline.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render the 30-day table, both empty states, and the awaiting cells</name>
  <read_first>
    - `components/admin/reporting-tab.tsx` (written in plan 07-02) — `ReportingTab`, `FunnelCards`,
      the loading/error/empty branching this task extends
    - `07-UI-SPEC.md` § Copywriting Contract (both empty-state pairs, the error copy, the two
      "Not yet sending" literals), § UI Considerations rows E2, § Spacing Scale (the `mb-6` gap
      between the card row and the table)
    - `components/admin/shortlist-table.tsx` lines 189-201 — the exact table shell to copy:
      `overflow-x-auto` wrapper, `table w-full text-sm`, and the `thead` row's
      `border-b border-gray-100 text-left text-gray-400 text-xs uppercase tracking-wider` classes
      with `px-4 py-3` cells
    - `lib/reporting-format.ts` (Task 1) — `formatReplyRate`
  </read_first>
  <action>
    Extend `components/admin/reporting-tab.tsx` with an exported `PerDayTable` and render it below
    `FunnelCards`, separated by the existing `mb-6` rhythm.

    Table shell copied from `shortlist-table.tsx` lines 189-201 verbatim in shape: an
    `overflow-x-auto` div wrapping `table w-full text-sm`, the same `thead` class string, the same
    `px-4 py-3` cell padding. Seven columns in this order: Date, Imported, Triaged, Scanned,
    Contacted, Reply rate, Booked. Exactly 30 `tbody` rows, newest first, always, including days
    where every integer is zero.

    Empty states, two distinct ones per the Copywriting Contract, both replacing the table (and,
    for the first, the card row too):
    - zero prospects ever imported: `No prospects imported yet` /
      `Run the importer to start building your funnel.` This is already the tab-level empty state
      from plan 07-02; the table renders nothing extra beneath it.
    - prospects exist but every one of the 30 days is zero across all counters:
      `No activity in the last 30 days` / `Every date below reads zero. Check the Shortlist tab to
      see where existing prospects currently stand.` rendered above the table, with the 30 zero
      rows still present, because the copy explicitly refers to them.

    Gated cells: while `payload.sentGateOpen` is false, every row's Reply rate and Booked cells
    render `— Not yet sending` with `text-gray-400 italic text-xs`, and the Imported / Triaged /
    Scanned / Contacted cells still render their real integers. When the gate is open, all cells
    render numbers and no other part of the UI changes. Reply rate always goes through
    `formatReplyRate`, never through inline string arithmetic, so a `null` can never reach the DOM
    as `NaN%` or `null%`.

    Because `REPLY_SIGNAL_AVAILABLE` is false, `replyRate` arrives `null` on every row even when
    the sent-gate is open. That cell therefore keeps the awaiting treatment past the gate flip.
    This is a strengthening of the UI-SPEC's E2 statement, not a deviation from it: it uses the
    same locked copy, changes no layout, and prevents the specific case the UI-SPEC's own reasoning
    warns about, which is a reply rate that reads as a result while being an absence.

    On the Booked cell, add a `title` attribute when `bookedByDomain` is greater than zero, reading
    how many of that day's bookings were matched by domain rather than by email address. This adds
    no visible copy and no layout, and it is what keeps an inferred booking distinguishable from a
    certain one at the point Joshua reads the number.

    The error banner keeps the existing wrapping treatment with no `truncate` and no
    `whitespace-nowrap`, so an unbounded `{detail}` string wraps inside its container.
  </action>
  <verify>
    <automated>npx vitest run app/admin/reporting-gate.test.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `PerDayTable` renders exactly 30 `<tr>` elements in `tbody` for any payload, asserted in the
      Task 3 test.
    - `grep -c 'overflow-x-auto' components/admin/reporting-tab.tsx` returns at least 1.
    - The component contains no arithmetic that turns a rate into a string; every rate render path
      calls `formatReplyRate`.
    - Both empty-state copy pairs appear verbatim as they are written in the UI-SPEC Copywriting
      Contract.
    - `npx tsc --noEmit` clean and `npm run build` succeeds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend the held-out render test with the E2 backstop assertions</name>
  <read_first>
    - `app/admin/reporting-gate.test.tsx` (written in plan 07-02) — the existing `describe` block
      per element, the fixture shape, and the `cleanup()` in `afterEach`
    - `07-UI-SPEC.md` § UI Considerations row E2/`partial`, and the closing paragraph "Why the two
      `partial` rows are `backstop` and not plain truths"
    - `07-VALIDATION.md` § Non-Negotiable Assertions item 1
  </read_first>
  <action>
    Add a second top-level `describe` to `app/admin/reporting-gate.test.tsx` covering the per-day
    table. Do not modify the existing funnel-card describe.

    Gate CLOSED, with a payload whose days carry non-zero `imported`, `triaged`, `scanned` and
    `contacted` values AND non-zero `booked` values: assert every row's Imported / Triaged /
    Scanned / Contacted cell renders its supplied integer; assert `Not yet sending` appears once
    per row in the Reply rate and Booked positions; assert the supplied booked integers do NOT
    appear anywhere in the rendered output. Supplying zeros here would prove nothing, so the
    fixture must supply real numbers the gate has to suppress.

    Gate OPEN, same fixture: assert the booked integers now render; assert `Not yet sending` no
    longer appears in the Booked column; assert the Imported / Triaged / Scanned / Contacted cells
    are unchanged from the closed-gate render, which is what "no other UI change" means.

    Reply rate with the gate OPEN: assert the cell still renders the awaiting treatment, because
    `REPLY_SIGNAL_AVAILABLE` is false. Add a comment on that assertion naming it as the guard
    against Phase 8 shipping a send path without a reply marker, so a future author who flips the
    constant knows this assertion is expected to change with it.

    Row count: assert exactly 30 `tbody` rows in every state, including the zero-activity empty
    state where the copy explicitly refers to those rows.

    Both empty states: assert the zero-prospects copy renders with no table, and the
    zero-30-day-activity copy renders WITH the 30 zero rows.
  </action>
  <verify>
    <automated>npx vitest run app/admin/reporting-gate.test.tsx && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/admin/reporting-gate.test.tsx` passes with at least 9 tests total across
      both describes.
    - The closed-gate table test supplies non-zero booked counts and asserts their absence from
      rendered output.
    - A test asserts exactly 30 `tbody` rows.
    - A test asserts the reply-rate cell keeps the awaiting treatment with `sentGateOpen: true`.
    - `npx vitest run` (all three projects) is green.
  </acceptance_criteria>
</task>

</tasks>

<flagged_assumptions>
- **FA-TRK-05.** `imported` counts `prospects.created_at`, which fires once per prospect at first
  import. A re-import that touches an existing prospect contributes nothing to that day's count.
  Whether Joshua reads "imported per run" as first-seen-ever or as touched-by-a-run is not resolved
  by CONTEXT.md, the ROADMAP, or REQUIREMENTS.md. First-seen is implemented because it is the only
  reading the existing schema can answer without adding a write to the import path, which D-7-10
  forbids. Surfaced, not dismissed.
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Modules (new)**
- `lib/lifecycle.ts` — `FineLifecycleState` (12 values), `LifecycleInputs`,
  `deriveLifecycleState()`, `FunnelGroup` (6 values), `FUNNEL_GROUPS`, `FUNNEL_CARD_ORDER`,
  `REPLY_SIGNAL_AVAILABLE`
- `lib/reporting-aggregates.ts` — `ReportingPayload`, `ReportingDay`, `FunnelCounts`,
  `getReportingData(sb)`, `utcDay(ts)`
- `lib/reporting-format.ts` — `formatReplyRate(rate)`
- `lib/retention-constants.ts` — `RetentionMode`, `RETENTION_MODE`, `RETENTION_MONTHS`,
  `RETENTION_TABLE_ALLOWLIST`, `ANONYMIZED_PROSPECT_FIELDS`
- `lib/retention.ts` — `RetentionResult`, `ExpiringProspect`, `selectExpiringProspects()`,
  `anonymizeProspects()`, `deleteProspects()`, `runRetention()`

**Components (new / extended)**
- `components/admin/reporting-tab.tsx` (new) — `ReportingTab`, `FunnelCards`, `PerDayTable`
- `components/admin/shortlist-table.tsx` (extended) — `stagePillStyles`, `StagePill`
- `app/admin/page.tsx` (extended) — `StatCard` becomes an export

**Routes**
- `GET /api/admin/reporting` (new), `GET /api/cron/retention` (new),
  `POST /api/webhooks/fillout` (extended), `GET /api/admin/shortlist` (payload gains `stage`)

**Schema**
- `prospects.booked_at`, `prospects.booked_match_method`,
  `prospects_booked_match_method_check`, `idx_prospects_booked_at`,
  `supabase/migrations/019_add_booked_at_to_prospects.sql`

**Type extensions**
- `ShortlistRow` gains `stage: FineLifecycleState`; `Tab` gains `"reporting"`

**Config**
- env `RETENTION_MODE` (default `dry-run`), env `RETENTION_MONTHS` (default `12`),
  `vercel.json` cron `{ "path": "/api/cron/retention", "schedule": "0 3 1 * *" }`

**Test infrastructure**
- vitest project `component` (jsdom, `**/*.test.tsx`); devDeps `jsdom`,
  `@testing-library/react`, `@testing-library/dom`

**Test files (new)**
- `components/admin/signal-chips.test.tsx`, `lib/lifecycle.test.ts`,
  `app/admin/reporting-gate.test.tsx`, `lib/reporting-aggregates.integration.test.ts`,
  `lib/reporting-format.test.ts`, `app/api/webhooks/fillout/route.integration.test.ts`,
  `lib/retention.integration.test.ts`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres -> aggregation | Row timestamps and match-method values cross into a reported figure |
| aggregation -> admin browser | Derived counts render as numbers Joshua acts on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-16 | Repudiation | `ReportingDay.replyRate` | high | mitigate | Three independent guards, each closing a different hole: `REPLY_SIGNAL_AVAILABLE` (no marker exists), `sentGateOpen` (nothing sent), and a zero-contacted check (division boundary). Enforced by the E2 assertions in Task 3 and the formatter unit tests, because a `0%` regression reads as a valid answer and passes code inspection (D-7-13). |
| T-07-17 | Tampering | the `scanned` per-day count | medium | mitigate | `.not("prospect_id", "is", null)` on the `scans` query. Without it the public scanner's own volume inflates a prospecting metric, which is D-7-16's scope line applied to the read side. Asserted by a fixture seeding a NULL-`prospect_id` scan and confirming it is not counted. |
| T-07-09 | Repudiation | the per-day Booked figure | medium | accept-with-control | A domain-inferred booking is a guess. `bookedByDomain` is carried in the payload and surfaced through the cell's `title` attribute so the inference stays visible without inventing copy outside the approved UI contract. Full per-booking provenance is readable from `prospects.booked_match_method` directly. |
| T-07-18 | Tampering | UTC vs local day bucketing | low | mitigate | One `utcDay()` function feeds all five timestamps, so a row's columns cannot describe different days. Asserted by two fixtures either side of midnight run under `TZ=Europe/Amsterdam`, which is the only condition that would expose a session-timezone dependency. |
</threat_model>

<verification>
- `npx vitest run` green across all three projects.
- `npx tsc --noEmit` clean, `npm run build` succeeds.
- `/api/admin/reporting` returns a payload with exactly 30 `days` entries.
- The Reporting tab renders funnel cards above a 30-row table, with the Reply rate and Booked
  columns showing the awaiting treatment.
</verification>

<success_criteria>
Joshua opens Reporting and reads how many prospects were imported, triaged, scanned and contacted
on each of the last 30 days. Reply rate does not show him a number, because there is no reply
signal to show, and the UI says so in words rather than in a zero.
</success_criteria>

<output>
Create `.planning/phases/07-lifecycle-reporting-retention/07-03-SUMMARY.md` when done.
</output>
