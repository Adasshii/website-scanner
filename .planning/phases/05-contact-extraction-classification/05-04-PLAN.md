---
phase: 05-contact-extraction-classification
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - lib/triage-candidates.ts
  - components/admin/shortlist-table.tsx
autonomous: false
requirements: [CON-05]
user_setup:
  - service: supabase
    why: "Migration 018 must be applied to the live project before the deployed code writes the new columns"
    dashboard_config:
      - task: "Run supabase/migrations/018_add_contact_classification.sql in the SQL Editor"
        location: "Supabase Dashboard -> SQL Editor (live project)"
  - service: railway
    why: "extractor.ts changes run on Railway; the harvest only reaches production scans after a Railway deploy"
    dashboard_config:
      - task: "git push to trigger the scanner-service Railway deploy"
        location: "Railway (scanner-service)"
  - service: vercel
    why: "The reconcile wiring + Shortlist pill ship with the Vercel app"
    dashboard_config:
      - task: "npx vercel --prod from the repo root"
        location: "Vercel (Prospect Radar app)"
must_haves:
  truths:
    - "a prospect whose only address is named-person is visibly flagged on the existing Shortlist, following the CRITICAL/UNREACHABLE pill pattern (CON-05)"
    - "the flag reads the stored contact_email_type — no new admin surface, no review queue"
    - "the full pipeline works end-to-end against real prospects: a scanned NL business shows a populated contact_email and, when named-person-only, the pill"
  artifacts:
    - "lib/triage-candidates.ts getShortlist selecting contact_email_type + ShortlistRow.contact_email_type"
    - "components/admin/shortlist-table.tsx NAMED-PERSON pill"
  key_links:
    - "contact_email_type flows ShortlistRow -> shortlist route -> admin page -> ShortlistTable with no route change once the column is selected and typed"
    - "the live migration must be applied BEFORE the two deploys, or the deployed writer hits missing columns"
---

<objective>
Surface the one case that needs a human — a prospect whose only contact is a named person — as a pill on the existing Shortlist, then apply the live migration, ship both services, and verify the whole extraction pipeline against real prospects.

Purpose: CON-05 is delivered as a Shortlist flag (D-5-02), not a new surface; and this is the phase's ship-and-prove gate — the extractor runs on Railway, so unlike prior phases the scanner-service deploy is mandatory this time. The 11 physiotherapy prospects released 2026-07-24 are the first real batch to verify recall against.
Output: the Shortlist reads + renders the named-person flag, migration 018 is live, both services are deployed, and a real batch confirms contact_email populates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-contact-extraction-classification/05-CONTEXT.md
@.planning/phases/05-contact-extraction-classification/05-RESEARCH.md
@lib/triage-candidates.ts
@components/admin/shortlist-table.tsx
@components/admin/signal-chips.tsx
@app/api/admin/shortlist/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shortlist NAMED-PERSON pill (getShortlist + ShortlistRow + table)</name>
  <files>lib/triage-candidates.ts, components/admin/shortlist-table.tsx</files>
  <read_first>
    - lib/triage-candidates.ts (ShortlistRow interface + getShortlist's explicit column select string — add contact_email_type to both)
    - components/admin/shortlist-table.tsx (the CRITICAL / UNREACHABLE pill cell at lines 156-180 and the badge-token class style; the pill is the pattern to mirror)
    - components/admin/signal-chips.tsx (severityStyles token palette — reuse the "major"/orange tokens for a needs-review flag, distinct from CRITICAL red and UNREACHABLE grey)
    - .planning/phases/05-contact-extraction-classification/05-CONTEXT.md (D-5-02 — pill/filter on the current Shortlist, no separate review queue)
  </read_first>
  <action>
    In lib/triage-candidates.ts: add `contact_email_type: string | null` to the `ShortlistRow` interface, and add `contact_email_type` to the explicit select column list in `getShortlist()`. Nothing else in the read path changes — the shortlist route, admin page state, and ShortlistTable props already pass `ShortlistRow[]` straight through, so the new column flows to the client automatically once selected and typed.
    In components/admin/shortlist-table.tsx: render a `NAMED-PERSON` pill for any row where `row.contact_email_type === 'named-person'`, in the same first (priority) cell as CRITICAL/UNREACHABLE and using the same rounded-full badge-token class shape, but the orange/amber "major" token palette (needs-human-review, visually distinct from CRITICAL red and UNREACHABLE grey). Do not add row-priority border treatment for it (it is a review flag, not a scan-priority signal). Add nothing else — no filter control, no new column, no new admin surface (D-5-02, ponytail). Add a one-line comment that keeping named-person prospects out of the default outreach flow is Phase 6's job (this pill is the visibility half of CON-05 only, per the orchestrator scope note).
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo TSC_OK && grep -q "contact_email_type" lib/triage-candidates.ts && grep -q "NAMED-PERSON" components/admin/shortlist-table.tsx && npx next lint --file components/admin/shortlist-table.tsx 2>/dev/null; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - `ShortlistRow` has `contact_email_type: string | null` and `getShortlist()` selects `contact_email_type`.
    - shortlist-table.tsx renders a `NAMED-PERSON` pill only when `contact_email_type === 'named-person'`, in the priority cell, with the orange/major token palette (not CRITICAL red, not UNREACHABLE grey).
    - No new filter, column, route, or admin surface is added.
    - `npx tsc --noEmit` green.
  </acceptance_criteria>
  <done>The Shortlist flags named-person-only prospects for manual review, reading the stored classification, with no new surface.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ship &amp; verify — live migration 018, Railway + Vercel deploys, real-batch extraction check</name>
  <action>BLOCKING human gate — the harness cannot deploy or read production. Joshua performs, in order: (1) apply supabase/migrations/018_add_contact_classification.sql in the live Supabase SQL Editor (never `supabase db push`); (2) deploy scanner-service to Railway via `git push` (mandatory this phase — extractor.ts runs on Railway); (3) deploy the app with `npx vercel --prod`; (4) run a real batch over the 11 physiotherapy prospects and confirm populated contact_email / classification and the NAMED-PERSON pill. Steps and expected results are detailed below.</action>
  <what-built>
    Migration 018 (contact columns + constraint), the extractor harvest (scanner-service), the reconcile wiring (Vercel app), and the Shortlist NAMED-PERSON pill are all written and unit/integration-tested locally. What remains is human-only: applying the migration to the live database and deploying both services (the harness cannot deploy or read production), then confirming real extraction.
  </what-built>
  <how-to-verify>
    Do these in order — the live migration MUST precede both deploys, or the deployed writer hits missing columns:
    1. Apply the migration live: open the Supabase Dashboard SQL Editor on the live project and run the full contents of supabase/migrations/018_add_contact_classification.sql. Do NOT run `supabase db push`. (It is idempotent — safe if partially applied.)
    2. Deploy scanner-service to Railway: `git push` so Railway rebuilds the scanner-service with the extractor harvest. This deploy is REQUIRED this phase (extractor.ts runs on Railway). Wait for the Railway deploy to go healthy.
    3. Deploy the app to Vercel: from the repo root run `npx vercel --prod` (this project is not git-connected to Vercel). Wait for the production URL to be live.
    4. Run a real batch: trigger a scan of the 11 physiotherapy prospects released 2026-07-24 (Run batch from the admin, or let the daily drain tick run), and wait for them to reach `done`.
    5. Confirm extraction on the live Shortlist (scan.adashi.io admin -> Shortlist tab): scanned rows show a `done` status, and prospects with a found contact now carry a `contact_email` (spot-check a few in Supabase: `contact_email`, `contact_email_type`, `commercial_contact_invited`, `sole_proprietorship` populated). At least one named-person-only prospect (if any in the batch) shows the NAMED-PERSON pill. A prospect that had no discoverable email keeps `contact_email` null (an accepted miss, e.g. click-to-reveal / PDF-only per RESEARCH pitfalls) — not a bug.
    Report the batch outcome: how many of the scanned prospects got a contact_email, the generic/named-person split, and any obvious misclassification, so the GENERIC_LOCALS list (RESEARCH Assumption A1) can be tuned if the first real batch shows a pattern.
  </how-to-verify>
  <resume-signal>Type "approved" once the migration is live, both services are deployed, and the real batch shows populated contact_email values (with the named-person split reported) — or describe what went wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored contact_email_type → admin UI | Rendered in the admin Shortlist; React auto-escapes, so extracted text cannot inject markup |
| deploy sequence | Applying code that writes new columns before the columns exist in prod would fail writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Tampering | prod writes to not-yet-existing columns | high | mitigate | The blocking checkpoint orders the live migration strictly before both deploys |
| T-05-11 | Elevation of Privilege | Shortlist data exposure | low | accept | Reuses the existing admin-session / x-admin-secret gate on the Shortlist route; no new endpoint introduced |
| T-05-12 | Information Disclosure | rendering extracted email in admin | low | accept | Admin-only surface; React escapes the value; the email is the business's own published address |
</threat_model>

<verification>
- `npx tsc --noEmit` green; the Shortlist renders the pill only for named-person rows.
- Migration 018 live, scanner-service deployed to Railway, app deployed to Vercel (checkpoint).
- A real batch produces populated `contact_email` values with a reported generic/named-person split (checkpoint).
</verification>

<success_criteria>
- Named-person-only prospects are flagged on the existing Shortlist via the stored classification (CON-05), no new surface.
- The full pipeline is live and proven against real prospects: extractor on Railway, wiring on Vercel, migration 018 on the live DB.
</success_criteria>

## Artifacts this phase produces (this plan)

- **Field:** `ShortlistRow.contact_email_type: string | null` (lib/triage-candidates.ts)
- **Behavior:** `getShortlist()` selects `contact_email_type`
- **UI label:** `NAMED-PERSON` pill (components/admin/shortlist-table.tsx), orange/major token, shown when `contact_email_type === 'named-person'`
- **Deploy state:** migration 018 applied live; scanner-service deployed to Railway; app deployed to Vercel (`npx vercel --prod`)

<output>
Create `.planning/phases/05-contact-extraction-classification/05-04-SUMMARY.md` when done.
</output>
