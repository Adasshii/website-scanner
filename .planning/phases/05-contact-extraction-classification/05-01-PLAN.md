---
phase: 05-contact-extraction-classification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/018_add_contact_classification.sql
  - types/scanner.ts
autonomous: true
requirements: [CON-03, CON-06, CON-07]
must_haves:
  truths:
    - "prospects can store a contact_email_type constrained to generic|named-person (CON-03)"
    - "prospects records commercial_contact_invited defaulting to false (CON-06)"
    - "prospects records sole_proprietorship as yes|no|unknown, defaulting to unknown (CON-07)"
    - "PageData can carry raw contact material (mailto hrefs, cfemail tokens, bounded page text) with no change to any existing extraction"
  artifacts:
    - supabase/migrations/018_add_contact_classification.sql
    - "types/scanner.ts ContactExtraction interface + PageData.contactExtraction field"
    - "types/scanner.ts ProspectRow.commercial_contact_invited + ProspectRow.sole_proprietorship"
  key_links:
    - "migration 018 CHECK constraints are the storage contract the Wave 2 pure module writes against"
    - "PageData.contactExtraction is the shape the Wave 2 extractor populates and the aggregator reads"
---

<objective>
Lay the storage + type foundation for contact extraction: one idempotent migration that adds the two new prospect columns and a CHECK constraint on the pre-existing `contact_email_type` column, plus the shared TypeScript types the extractor (Wave 2) writes and the aggregator (Wave 2/3) reads.

Purpose: every later plan writes into these columns and this shape; defining them once, first, keeps the pure module, the extractor, and the wiring in agreement and lets the Wave 3 integration test see real columns on the local Supabase.
Output: migration 018 applied to the LOCAL Supabase only, and `types/scanner.ts` extended with `ContactExtraction`, `PageData.contactExtraction`, and two new `ProspectRow` fields.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-contact-extraction-classification/05-CONTEXT.md
@.planning/phases/05-contact-extraction-classification/05-RESEARCH.md
@supabase/migrations/010_create_prospects.sql
@supabase/migrations/017_add_scan_status.sql
@types/scanner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 018 — contact-classification columns + constraint, local apply</name>
  <files>supabase/migrations/018_add_contact_classification.sql</files>
  <read_first>
    - supabase/migrations/017_add_scan_status.sql (idempotent `add column if not exists` + `check` convention, header-comment style)
    - supabase/migrations/010_create_prospects.sql (existing `contact_email` / `contact_email_type` columns this migration constrains; RLS already enabled here)
    - .planning/phases/05-contact-extraction-classification/05-RESEARCH.md (Recommended Project Structure: migration 018 is the single idempotent, dashboard-applied schema change)
  </read_first>
  <action>
    Create supabase/migrations/018_add_contact_classification.sql, all statements idempotent and safe to re-run (matching migration 017's style and header-comment convention).
    Add three schema changes to the existing `prospects` table:
    (1) `commercial_contact_invited boolean not null default false` via `add column if not exists` — CON-06, defaults to "no" per D-5-R2.
    (2) `sole_proprietorship text not null default 'unknown'` via `add column if not exists`, with an inline `check (sole_proprietorship in ('yes','no','unknown'))` — CON-07 three-state signal per D-5-01; default 'unknown' is treated cautiously (as personal data) downstream.
    (3) A CHECK constraint named `prospects_contact_email_type_check` on the pre-existing `contact_email_type` column (created null in migration 010), added inside a guarded `do $$ ... $$` block that first tests `pg_constraint.conname` so re-running is a no-op. The constraint allows NULL (not-yet-extracted) OR one of `'generic'`,`'named-person'` — CON-03.
    Do NOT re-enable RLS (already enabled in migration 010) and do NOT add any policy (service-role-only convention). Do NOT touch `contact_email` itself.
    After writing the file, apply it to the LOCAL Supabase only, using this project's local-apply method: `supabase db reset` (which replays supabase/migrations/*, now including 018). Do NOT run `supabase db push` and do NOT apply to production — the live apply is a blocking checkpoint in plan 05-04.
  </action>
  <verify>
    <automated>test -f supabase/migrations/018_add_contact_classification.sql && grep -q "commercial_contact_invited" supabase/migrations/018_add_contact_classification.sql && grep -q "sole_proprietorship" supabase/migrations/018_add_contact_classification.sql && grep -q "prospects_contact_email_type_check" supabase/migrations/018_add_contact_classification.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/018_add_contact_classification.sql exists.
    - Every DDL statement uses `add column if not exists` or a `do $$`-guarded `pg_constraint` existence check (running the file twice against the same DB produces no error and no duplicate constraint).
    - `commercial_contact_invited` is `boolean not null default false`.
    - `sole_proprietorship` is `text not null default 'unknown'` and its CHECK permits only `yes`,`no`,`unknown`.
    - Constraint `prospects_contact_email_type_check` permits NULL or `generic`/`named-person` and rejects any other string.
    - The SQL was applied to the local Supabase (127.0.0.1:54321) via `supabase db reset`, not to production, and not via `supabase db push`.
  </acceptance_criteria>
  <done>The three schema changes exist as one idempotent migration and are present on the local Supabase; production is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend shared types — ContactExtraction, PageData.contactExtraction, ProspectRow columns</name>
  <files>types/scanner.ts</files>
  <read_first>
    - types/scanner.ts (existing `PageData` interface ~lines 65-123 and `ProspectRow` interface ~lines 382-408; note `contact_email` / `contact_email_type` already declared on ProspectRow)
    - .planning/phases/05-contact-extraction-classification/05-RESEARCH.md (Architecture Patterns — Pattern 1 raw-material capture; the extractor harvests DOM-only material, the pure module parses it)
  </read_first>
  <action>
    In types/scanner.ts add one new exported interface `ContactExtraction` with exactly three fields: `mailtoHrefs: string[]` (raw `href` values from mailto anchors), `cfemailTokens: string[]` (raw `data-cfemail` attribute values), and `contactText: string` (bounded visible page text). Add an optional `contactExtraction?: ContactExtraction` field to `PageData` (optional so legacy scans already persisted in `scans.pages` remain valid and the aggregator can treat its absence as "no candidates").
    Extend `ProspectRow` with two non-optional fields matching migration 018's not-null-with-default columns: `commercial_contact_invited: boolean` and `sole_proprietorship: string`. Leave the existing `contact_email` / `contact_email_type` fields as-is.
    Add a short comment noting these three raw fields are populated by scanner-service `extractPageData()` (plan 05-02) and consumed by `lib/contact-extraction.ts` `aggregateContacts()` — Node-side parsing keeps the browser context a thin harvester (no cfemail/regex logic crossing the boundary).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -5; npx tsc --noEmit && echo TSC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `ContactExtraction` is exported from types/scanner.ts with fields `mailtoHrefs: string[]`, `cfemailTokens: string[]`, `contactText: string`.
    - `PageData` has an optional `contactExtraction?: ContactExtraction` field.
    - `ProspectRow` has `commercial_contact_invited: boolean` and `sole_proprietorship: string`.
    - `npx tsc --noEmit` is green (no type errors introduced across the shared types).
  </acceptance_criteria>
  <done>The shared type surface for contact extraction exists and compiles; extractor and aggregator can be written against it in Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extracted string → prospects row | Untrusted third-party website text/emails eventually land in these columns; the schema is the last structural guard on their SHAPE (not length — length is bounded app-side in plan 05-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | contact_email_type column | medium | mitigate | CHECK constraint `prospects_contact_email_type_check` rejects any classification value other than generic/named-person/NULL, so a wiring bug cannot persist a garbage class |
| T-05-02 | Tampering | sole_proprietorship column | low | mitigate | Inline CHECK restricts to yes/no/unknown; NOT NULL default 'unknown' means an unwritten row is treated cautiously (as personal data) by construction |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: none proposed); nothing to verify |
</threat_model>

<verification>
- `supabase db reset` replays cleanly through 018 with no error.
- Re-running 018's SQL a second time against the same DB is a no-op (idempotency).
- `npx tsc --noEmit` green.
</verification>

<success_criteria>
- Migration 018 exists, is idempotent, and is applied to the local Supabase only.
- The three CHECK/default guarantees (contact_email_type, sole_proprietorship, commercial_contact_invited) hold on the local DB.
- Shared types compile and expose `ContactExtraction`, `PageData.contactExtraction`, and the two new `ProspectRow` fields.
</success_criteria>

## Artifacts this phase produces (this plan)

- **File:** `supabase/migrations/018_add_contact_classification.sql`
- **Column:** `prospects.commercial_contact_invited` (boolean, not null, default false)
- **Column:** `prospects.sole_proprietorship` (text, not null, default 'unknown', CHECK yes|no|unknown)
- **Constraint:** `prospects_contact_email_type_check` (contact_email_type NULL or generic|named-person)
- **Type:** `ContactExtraction { mailtoHrefs: string[]; cfemailTokens: string[]; contactText: string }` (exported, types/scanner.ts)
- **Field:** `PageData.contactExtraction?: ContactExtraction`
- **Field:** `ProspectRow.commercial_contact_invited: boolean`
- **Field:** `ProspectRow.sole_proprietorship: string`

<output>
Create `.planning/phases/05-contact-extraction-classification/05-01-SUMMARY.md` when done.
</output>
