---
phase: 08-send-gated
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - lib/send-audit.ts
  - lib/send-audit.integration.test.ts
  - app/api/admin/outreach/audit/route.ts
  - components/admin/outreach-row-panel.tsx
  - components/admin/outreach-row-panel.test.tsx
  - lib/outreach-isolation.test.ts
autonomous: true
requirements: [CMP-12, SND-01, SND-03, SND-04]

estimate:
  tokens: 46000
  raw_tokens: 23000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "For any prospect that has been sent to, the admin surface renders the full legal-basis record in one expand, without reading a log file (CMP-12)."
    - "The audit answer is served from the immutable record alone, so it cannot drift as prospect or draft rows change afterwards."
    - "A test fails if any module in the outreach path gains an import of a mail-sending client or the public scanner's mail module (SND-03)."
    - "A test fails if a mail-sending dependency is added to package.json, so no dispatch provider can enter the path by install (SND-01)."
    - "The written acceptable-use verification artifact exists and is pinned by a test rather than remembered (SND-04)."
  artifacts:
    - lib/send-audit.ts
    - app/api/admin/outreach/audit/route.ts
    - "The audit block in components/admin/outreach-row-panel.tsx"
    - lib/outreach-isolation.test.ts
    - lib/send-audit.integration.test.ts
    - components/admin/outreach-row-panel.test.tsx
  key_links:
    - "send_records -> getSendAudit -> the audit route -> the panel block: one denormalised read answers the question, with no join back to mutable prospect or draft rows."
    - "lib/outreach-isolation.test.ts -> the enumerated outreach file set: the standing guard that keeps SND-01 and SND-03 true after this phase closes."
    - ".planning/research/SEND-CHANNEL.md -> lib/outreach-isolation.test.ts: the SND-04 written verification, asserted to exist rather than assumed."
---

<objective>
Close the phase with the answer and the guard. The answer is CMP-12: one expand shows why a given
business was allowed to be emailed, read straight off the immutable record. The guard is SND-01,
SND-03 and SND-04: automated checks that fail the moment a dispatch provider or the public scanner's
mail module enters the outreach path, plus a pin on the written verification artifact.

Purpose: manual send makes these three requirements trivially true today. Trivially true is not the
same as proven, and the value is that they stay true after this phase closes and nobody remembers
why. The existing public scanner's transactional mail is the thing being protected, and it earns
money.

Output: one lib reader, one read-only route, one panel block, and three test files that turn three
prose claims into failing builds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-send-gated/08-CONTEXT.md
@.planning/phases/08-send-gated/08-01-PLAN.md
@.planning/phases/08-send-gated/08-02-PLAN.md
@.planning/research/SEND-CHANNEL.md

Source of truth for what this plan consumes:
@lib/send-record.ts
@lib/outreach-queue.ts
@components/admin/outreach-row-panel.tsx
@app/api/admin/outreach/send/route.ts
</context>

<hard_scope_fence>
Unchanged and still binding. This plan renders legal values; it does not author, interpret, or
summarise them:

- The audit block displays `legal_basis`, `lia_version` and `tw_exemption_claimed` as labelled
  fields with the stored value beside each label. Do not compose a sentence, a justification, or an
  explanation out of them. A field list is the deliverable; prose about legal basis is counsel's.
- Do not add a fallback label for a NULL or unexpected legal value. There is no such row today,
  because the gate refuses before a record can be written.
- Do not edit the Article 14 notice constants in `lib/draft-prompt.ts`.
- Do not author or version an LIA.
</hard_scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getSendAudit and the read-only audit route</name>
  <read_first>
    - supabase/migrations/020_create_send_records.sql (every `send_records` column name and type, which this reader maps one to one)
    - lib/send-record.ts (the field mapping `markAsSent` writes, so the reader's camelCase names correspond exactly)
    - app/api/admin/outreach/send/route.ts (the literal `x-admin-secret` check, the `serializeError` helper, and the response shape conventions this route mirrors)
    - lib/send-gate.integration.test.ts (the local-Supabase env override header and the FK-safe chunked cleanup this suite mirrors)
    - app/api/admin/shortlist/route.ts (the existing GET-with-query-param convention on this admin surface)
  </read_first>
  <precondition>Local Supabase is running at 127.0.0.1:54321 with migrations 001 through 020 applied. Rows this suite writes into `send_records` cannot be deleted afterwards, so it must use throwaway prospects under its own fixture prefix and say so in its file header.</precondition>
  <files>lib/send-audit.ts, lib/send-audit.integration.test.ts, app/api/admin/outreach/audit/route.ts</files>
  <behavior>
    - `getSendAudit(sb, prospectId)` returns an empty array for a prospect with no send record, never null and never a thrown error.
    - It returns one entry per `send_records` row for that prospect, newest first by sent timestamp.
    - Each entry carries all sixteen stored values, camelCased, with no field derived from any other table.
    - Two prospects with records do not bleed into each other's results.
    - The route returns 401 without the admin secret, 400 for a missing or non-UUID prospect id, and 200 with an `entries` array otherwise.
  </behavior>
  <action>
Write `lib/send-audit.integration.test.ts` first, asserting every behavior above against local
Postgres, then write the module and the route.

`lib/send-audit.ts` exports `SendAuditEntry`, an interface with `sendRecordId`,
`outreachMessageId`, `prospectId`, `sentAt`, `resolvedEmail`, `resolvedEmailType`, `subjectSent`,
`bodySent`, `legalBasis`, `liaVersion`, `twExemptionClaimed`, `firstContactNoticeIncluded`,
`isFirstContact`, `approvedBy`, `suppressionCheckedAt` and `suppressionHit`. It exports
`getSendAudit(sb: SupabaseClient, prospectId: string): Promise<SendAuditEntry[]>`, which issues one
query against `send_records` filtered on `prospect_id`, ordered by the sent timestamp descending, and
maps each row straight into the entry shape.

One query and no join is the design, not a shortcut. Every value CMP-12 needs was denormalised onto
the record at mark time precisely so the answer cannot drift when the prospect row, the draft text or
the regime config change afterwards. Write that reason into the module header, and state that adding
an embedded select against another table here would reintroduce exactly the drift the immutable
record exists to prevent.

`app/api/admin/outreach/audit/route.ts` exports `runtime = "nodejs"` and a GET handler. Repeat the
literal `x-admin-secret` comparison against `process.env.ADMIN_SECRET` inline, matching the existing
per-handler convention. Read `prospectId` from `request.nextUrl.searchParams`, reject a missing value
or one that fails a UUID format test with 400, and otherwise return 200 with `{ entries }` from
`getSendAudit`. Reuse the `serializeError` shape the sibling send route already uses for its 500
path. This handler is read-only: it must contain no insert, update or delete.
  </action>
  <verify>
    <automated>npx vitest run lib/send-audit.integration.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/send-audit.integration.test.ts` exits 0 with at least 5 tests passing, one per listed behavior.
    - `npx tsc --noEmit` exits 0.
    - `grep -v '^ \*' lib/send-audit.ts | grep -c "sb.from("` returns exactly 1. The answer costs one query.
    - `grep -v '^ \*' lib/send-audit.ts | grep -c "prospects ("` returns 0 and the same filtered grep for `outreach_messages (` returns 0. No join back to a mutable row.
    - `grep -v '^ \*' lib/send-audit.ts | grep -c ".insert(\|.update(\|.delete("` returns 0.
    - `grep -c "SendAuditEntry" lib/send-audit.ts` returns at least 2, and all sixteen listed field names appear in the interface.
    - `grep -c "x-admin-secret" app/api/admin/outreach/audit/route.ts` returns at least 1.
    - `grep -v '^ \*' app/api/admin/outreach/audit/route.ts | grep -c ".insert(\|.update(\|.delete("` returns 0.
    - `curl -s -o /dev/null -w '%{http_code}' 'http://localhost:3000/api/admin/outreach/audit?prospectId=abc'` returns 401 without the secret header, and returns 400 with the correct secret header and that same non-UUID value.
    - A test asserts two seeded prospects each return only their own entries.
  </acceptance_criteria>
  <done>One authenticated GET returns every stored fact about every send to a given prospect, read from the immutable record alone.</done>
</task>

<task type="auto">
  <name>Task 2: "Why were we allowed to email this business?" in the panel</name>
  <read_first>
    - components/admin/outreach-row-panel.tsx (the file being modified; the prepared-state block from 08-02, the `role="alert"` banner, and the two-column grid layout)
    - lib/send-audit.ts (the `SendAuditEntry` field names this block renders)
    - lib/outreach-queue.ts (`OutreachQueueRow`, which supplies `prospectId` and `status` to this block)
    - components/admin/shortlist-table.test.tsx (the existing component-test convention: jsdom project, Testing Library render, no dev server)
    - vitest.config.ts (the `.test.tsx` glob and the jsdom project, so the new component test lands in the right project)
  </read_first>
  <files>components/admin/outreach-row-panel.tsx, components/admin/outreach-row-panel.test.tsx</files>
  <action>
Add an audit block to the expanded review panel, rendered only when `row.status === "sent"`. On
expand it fetches `/api/admin/outreach/audit?prospectId={row.prospectId}` with the same
`x-admin-secret` header the panel's other calls send, holds the returned entries in local state, and
renders them newest first.

Head the block with the literal question `Why were we allowed to email this business?`, which is the
CMP-12 bar stated in the UI rather than in a document. Under it, render each entry as a definition
list with one labelled row per stored value, in this order: sent at, resolved address, address
classification, first contact, legal basis, LIA version, Tw exemption claimed, Article 14 notice
included, approved by, suppression checked at, suppression result. Render the booleans as `Yes` and
`No` rather than raw literals. Below the list, render the recorded subject and the recorded body in
read-only fields, labelled as the content actually recorded for this send.

Render the values as stored, beside their labels. Do not build a sentence out of them, do not add an
interpretation line, and do not add a reassuring summary. The list is the answer.

Handle three states explicitly: loading, an empty array, and a fetch failure. An empty array for a
`sent` row means the message status advanced without a record, which is the one inconsistency 08-02
Task 1 can leave behind, so render it as a visible warning naming that condition rather than as a
blank block. Route a fetch failure into the existing `actionError` banner.

Write `components/admin/outreach-row-panel.test.tsx` covering: the block is absent for a row whose
status is `approved`; the block renders all eleven labels for a `sent` row when the fetch resolves
with one entry; booleans render as `Yes` and `No`; and the empty-array case renders the
record-missing warning. Stub `global.fetch` per test. Follow the render-and-query conventions in
`components/admin/shortlist-table.test.tsx` rather than inventing a second style.
  </action>
  <verify>
    <automated>npx vitest run components/admin/outreach-row-panel.test.tsx && npx tsc --noEmit && npm run build</automated>
    <human-check>No prospect can reach `sent` status while the legal gate is shut, so this block cannot be exercised against real data yet. Confirm instead that expanding an approved row shows no audit block and no console error.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run components/admin/outreach-row-panel.test.tsx` exits 0 with at least 4 tests passing.
    - `npx tsc --noEmit` exits 0 and `npm run build` compiles.
    - `grep -c "Why were we allowed to email this business?" components/admin/outreach-row-panel.tsx` returns 1.
    - `grep -c "outreach/audit" components/admin/outreach-row-panel.tsx` returns at least 1.
    - All eleven label strings appear in the component file, each matched by its own `grep -c` returning at least 1.
    - `grep -c "legalBasis" components/admin/outreach-row-panel.tsx` returns at least 1 and `grep -c "liaVersion"` returns at least 1 and `grep -c "twExemptionClaimed"` returns at least 1.
    - `grep -c "window.confirm" components/admin/outreach-row-panel.tsx` returns 0 and `grep -c "window.alert"` returns 0.
    - A test asserts the block is not in the document for a row with status `approved`.
    - A test asserts the record-missing warning renders when the fetch resolves with an empty `entries` array for a `sent` row.
  </acceptance_criteria>
  <done>Expanding a sent row answers the compliance question on screen in one read, from stored values only.</done>
</task>

<task type="auto">
  <name>Task 3: The isolation guard for SND-01, SND-03 and SND-04</name>
  <read_first>
    - lib/email.ts (the public scanner's transactional mail module, which is the asset being protected and which the outreach path must never reach)
    - lib/outreach-queue.ts (its header comment already declares which imports it must never gain; this test makes that declaration executable and extends it across the whole outreach path)
    - .planning/research/SEND-CHANNEL.md (the SND-04 written verification, in particular its section 0 heading recording the manual-send decision)
    - .planning/REQUIREMENTS.md lines 93 to 129 (the SND-01 through SND-04 text with their 2026-08-04 annotations)
    - package.json (the current dependency list this test pins)
  </read_first>
  <files>lib/outreach-isolation.test.ts</files>
  <action>
Write one source-assertion test file that turns three prose claims into failing builds. It reads
files from disk with `node:fs`'s `readFileSync` relative to `process.cwd()` and asserts on their
contents. It starts no server, opens no database connection, and belongs to the node unit project,
not the integration project, so it runs on every `npx vitest run`.

Declare a module-level `OUTREACH_PATH_FILES` array naming, by exact path, every file that
participates in producing or dispatching an outreach message: the four Phase 8 lib modules
(`lib/send-gate.ts`, `lib/opt-out-link.ts`, `lib/send-record.ts`, `lib/send-audit.ts`), the Phase 6
drafting and queue modules (`lib/outreach-queue.ts`, `lib/draft-generator.ts`, `lib/draft-prompt.ts`,
`lib/draft-metric-selector.ts`, `lib/draft-on-scan-complete.ts`), and the three admin routes
(`app/api/admin/outreach/route.ts`, `app/api/admin/outreach/send/route.ts`,
`app/api/admin/outreach/audit/route.ts`). Assert first that every listed file exists, so a rename
fails the guard loudly instead of silently shrinking the set it checks.

Before matching, strip comment lines from each file: drop every line whose trimmed form begins with
a line-comment marker or a block-comment continuation marker. Match only the remaining lines. This
matters because the modules in the set are expected to explain in their comments why they avoid
these dependencies, and a matcher that read comments would fail on its own documentation.

Then assert three things.

First, SND-03 isolation: no non-comment line in any listed file references any member of a
`BANNED_MAIL_TOKENS` array. The exact tokens are enumerated in the acceptance criteria below; put
that same list in the test as a named constant with a comment stating that the public scanner's
transactional mail is a working, revenue-earning system and this list is what keeps an outreach
failure from reaching it.

Second, SND-01, no dispatch channel by install: parse `package.json` and assert that neither
`dependencies` nor `devDependencies` contains any key in a `BANNED_MAIL_PACKAGES` array, enumerated
in the acceptance criteria. Assert separately that the currently installed transactional-mail
dependency the public scanner uses is still present in `dependencies`, so this test cannot be
satisfied by deleting the public scanner's mail capability, which would be a regression rather than
a fix.

Third, SND-04, the written verification: assert `.planning/research/SEND-CHANNEL.md` exists and
contains its section 0 status heading recording the manual-send decision. The requirement is that
the acceptable-use position is verified in writing before anything is built against it. Nothing is
built against a provider, and this pins the artifact that records why.

Add a file header stating what breaks if a future change makes this file fail: not a style rule, but
the separation that keeps an outreach problem from taking down the live product's mail.
  </action>
  <verify>
    <automated>npx vitest run lib/outreach-isolation.test.ts && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/outreach-isolation.test.ts` exits 0 with at least 4 tests passing.
    - `npx vitest run` over the whole suite exits 0.
    - `BANNED_MAIL_TOKENS` in the test contains exactly these six strings: `from "resend"`, `require("resend")`, `@/lib/email`, `nodemailer`, `createTransport`, `smtp.`
    - `BANNED_MAIL_PACKAGES` in the test contains exactly these five strings: `nodemailer`, `@sendgrid/mail`, `mailgun.js`, `@aws-sdk/client-ses`, `postmark`.
    - `grep -c "OUTREACH_PATH_FILES" lib/outreach-isolation.test.ts` returns at least 2, and all twelve file paths appear in that array.
    - `grep -c "SEND-CHANNEL.md" lib/outreach-isolation.test.ts` returns at least 1.
    - `grep -c "readFileSync" lib/outreach-isolation.test.ts` returns at least 1 and `grep -c "createServerClient" lib/outreach-isolation.test.ts` returns 0. The guard opens no database connection.
    - Temporarily adding a line importing the public scanner's mail module into `lib/send-gate.ts` makes `npx vitest run lib/outreach-isolation.test.ts` exit non-zero; reverting it makes the suite pass again. Prove the guard fails first, then revert.
    - Temporarily adding a comment line naming a banned token to `lib/send-gate.ts` leaves the suite passing, proving the comment stripping works; revert it.
    - `git diff --numstat package.json` reports 0 changed lines across the whole of Phase 8.
  </acceptance_criteria>
  <done>Three requirements that were true by argument are now true by test, and the test has been proven to fail when the property it guards is violated.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by this plan (plan 08-03 of 3):

| Kind | Symbol or path |
|------|----------------|
| File | `lib/send-audit.ts` |
| Type | `SendAuditEntry` |
| Function | `getSendAudit(sb, prospectId)` |
| Route | `GET /api/admin/outreach/audit?prospectId=` |
| UI | "Why were we allowed to email this business?" block in `components/admin/outreach-row-panel.tsx` |
| Test | `lib/send-audit.integration.test.ts` |
| Test | `components/admin/outreach-row-panel.test.tsx` |
| Test | `lib/outreach-isolation.test.ts` |
| Const | `OUTREACH_PATH_FILES`, `BANNED_MAIL_TOKENS`, `BANNED_MAIL_PACKAGES` |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser to audit API | A read of personal data (resolved contact addresses and their classification) plus the full recorded message body. |
| outreach code to the public scanner's mail credentials | The separation SND-03 exists to enforce; the scanner's transactional mail is live and earning. |
| repository to future contributors | The point where a dependency or an import could quietly reintroduce a dispatch provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-01 | Spoofing | `GET /api/admin/outreach/audit` | high | mitigate | Literal `x-admin-secret` comparison inline in the handler, 401 with no body detail on mismatch, asserted by an unauthenticated curl. |
| T-08-03-02 | Information disclosure | audit response contents | medium | mitigate | The endpoint requires the admin secret, accepts only a UUID prospect id, and returns rows scoped to that single prospect. There is no list-all variant and none should be added. |
| T-08-03-03 | Tampering | audit route reaching a write path | medium | mitigate | The handler is read-only, gated by acceptance greps that fail if an insert, update or delete appears in either the route or the reader module. |
| T-08-03-04 | Tampering | outreach path reaching the public scanner's mail credentials | high | mitigate | `lib/outreach-isolation.test.ts` fails on any banned import token in any of twelve enumerated outreach files, and the guard is proven to fail first by a deliberate temporary violation before being reverted. |
| T-08-03-05 | Tampering | a dispatch provider entering by install | high | mitigate | The same test pins `package.json` against five named mail packages, and separately asserts the public scanner's own mail dependency is still present so the check cannot be satisfied by breaking the scanner. |
| T-08-03-06 | Repudiation | the SND-04 verification artifact disappearing | medium | mitigate | The test asserts `.planning/research/SEND-CHANNEL.md` exists and still carries its section 0 manual-send status heading. |
| T-08-03-07 | Denial of service | unbounded audit read | low | accept | One indexed lookup scoped to one prospect, on a single-tenant admin surface at 10 to 50 sends per week. Pagination would be over-built at this scale. |
| T-08-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no packages. `git diff --numstat package.json` reporting 0 changed lines across the whole phase is an acceptance criterion of Task 3, so the Package Legitimacy Gate is not triggered and cannot be triggered silently. |
</threat_model>

<verification>
- `npx vitest run` exits 0 across the whole suite.
- `npx tsc --noEmit` exits 0 and `npm run build` compiles.
- The isolation guard has been proven to fail on a deliberate violation and to pass again after revert. A guard never seen failing is not a guard.
- `package.json` is byte-identical to its pre-phase state.
- With the legal gate still shut, no `send_records` row exists, so the audit block is unreachable in practice. That is the expected end state of this phase.

<human-check>
Walk the whole phase once by hand in the admin Outreach tab: expand an approved draft, press Prepare
send, read the `legal-basis-unset` refusal, confirm no copy block appears, switch to the `sent`
filter and confirm it is empty. The mechanism is complete and the send stays shut by its own gates,
not by a note in a file. Nothing further ships until counsel supplies `legal_regimes.legal_basis` and
sets `article_14_notice_approved`.
</human-check>

## Open item carried to counsel, no code change

`ARTICLE_14_NOTICE_EN` and `ARTICLE_14_NOTICE_NL` in `lib/draft-prompt.ts` both state that the
message passes through an email-delivery provider acting as a data processor. That was true when the
notices were drafted in Phase 6 and is no longer true under the 2026-08-04 manual-send decision.
Correcting it is notice wording, which is counsel's output and outside this phase's fence, so nothing
in Phase 8 touches it. Recorded here so the discrepancy is carried, not lost.
</verification>

<success_criteria>
- One authenticated expand answers "why were we allowed to email this business?" from the immutable record (CMP-12).
- A test fails if the outreach path gains a mail-sending import (SND-03).
- A test fails if a mail-sending dependency is installed, and separately asserts the public scanner's own mail capability survives (SND-01).
- The written acceptable-use verification artifact is pinned by test (SND-04).
- No legal content authored anywhere in this plan.
</success_criteria>

<output>
Create `.planning/phases/08-send-gated/08-03-SUMMARY.md` when done.
</output>
