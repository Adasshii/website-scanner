[build]
dockerfilePath = "scanner-service/Dockerfile"
watchPatterns = ["scanner-service/**", "types/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
